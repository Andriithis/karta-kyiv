# Стан роботи над картою — 24.09.2026

Коротка передача для нового чату. Центральний документ — `RISHENNYA.md`
(він же `claude/rishennya-2026-09-15.md` у проєкті); чинний порядок —
його розділ «Порядок».

## Що це

`Andriithis/karta-kyiv` — карта правопорушень і ризиків Києва з рішень
ЄДРСР. **Некомерційний навчальний інструмент для Академії патрульної
поліції.** Публікується на GitHub Pages, перезбирається діями «Оновлення
карти».

## Ролі

- **Основний чат (центральний напрям)** — аудит, рішення, узгодження всіх
  напрямів між собою, завдання виконавцю. Андрій затверджує. Сам у
  репозиторій код не пише.
- **Чати напрямів** (POP-дослідження, визначення проблем тощо) кладуть свої
  файли в корінь репозиторію. Їхні рішення набирають сили, коли основний чат
  записав їх у `RISHENNYA.md` (див. «Як узгоджуються документи» на початку).
- **Claude Code** у репозиторії — впровадження лише за `ZAVDANNYA-*.md`.
  Плани й звіти Андрій пересилає в основний чат.
- **Правило вигляду:** будь-яка зміна вигляду — спершу макет «було / стане»
  і затвердження Андрія.

## Зроблено

1. Кольори маркерів — `f1196b0`.
2. Теми, палітра, підкладка CARTO за ключем — `7895e75`, `75a10c7`.
3. Маркери на великому зумі — `1964629`, `359f6e3`.
4. Панель — `33c4699`; панель-навігатор за макетом — `c479552`.
5. Спільна логіка винесена (`e061582`), каркас MapLibre-збірки
   `kyiv-gl.html` (`0c234f5`).

## Зараз — 24.09

- **Claude Code, після GPT:**
  - суть `sut()` відкочено: на карті фабула з проходу з безпечними
    вирізаннями (`step1c_teksty.pokaz`), без опису 1 732 справи замість
    5 193, шкідливих 0 з 30;
  - відкрита панель іде за фільтром;
  - «Резюме» й «Аналіз» рахують 61 989 подій, як карта
    (`step3_map.vybir`).

  Звіт — `ZVIT-GPT.md`. **Можна натискати «Оновлення карти».**
- **Чекає центрального чату:** `PLAN-BAZA.md` — власна база подій і
  повні тексти в Releases. Далі не починав.

- **GPT, пункт 3:** суть правилами на карті й у панелі (`6f0c413`), межі
  500/900 по реченню. Вибірка: 10 добрих із 21, ще 4 косметичні;
  обмеження для центрального чату — `ZVIT-GPT-SUT.md`, `ZVIT-GPT.md`.

- **GPT, пункт 2:** рік і час доби — кнопки-чипи; перевірено вибір
  2025 + ніч (13 → 1 справа на Соборності, 2). Звіт — `ZVIT-GPT.md`.

- **GPT, пункт 1:** «Що поруч» дороблено й перевірено в браузері;
  перемикач, скидання при зміні вибору, підказка при натисканні.
  Подробиці й обмеження — `ZVIT-GPT.md`.

- **Прохід:** правило `v3` (частина `data/teksty/2026-09-24-02`): місце
  події в НАР — не там, звідки замовляли (265 подій змінили місце); хвіст
  «Відповідальність за вказане … передбачена ст.»; час «приблизно об 11
  год.» (+778 подій з часом). Бот щодня бере по 30 000 — дожене ДОР.
- **Карта читає частини проходу — запушено:** адреса, фабула, клас,
  шкідлива, дата події від 2023; у панелі — дата й час події (або дата
  рішення з позначкою «рішення»), сортування за датою події; рядка про
  сусідні номери немає; «Усі рішення (N)» і панель — за поточним фільтром.
- **Чекає Андрія:**
  - 20 прикладів «фабула v2 → суть» і оцінка варіанта з мовною моделлю —
    `ZVIT-SUT.md`; `src/sut.py` є, на карту не підключено;
  - знімки `site/znimky/8-rik-*.png` — рік і час доби в «Розширено»
    (код — локальна гілка `rik-chas`, не запушена);
  - знімки `8-poruch-*.png` — значки «Що поруч» (гілка `poruch-ikony`);
  - план кроку 7 — `PLAN-KROK7.md` (радіус для кожного типу за RTM,
    джерела — `NAUKA.md`, розд. 3).
- **Сайт перезбирається лише кнопкою** «Оновлення карти» (або щопонеділка):
  пуш у main сам його не запускає.

## Крок 6, прохід по текстах (оновлено 23.09 увечері)

Завдання — `ZAVDANNYA-TEKSTY.md`, план — `PLAN-TEKSTY.md`, рішення —
`RISHENNYA.md`, розд. 21. Звіти — `ZVIT-TEKSTY-1.md` (витяг фабул,
позначка шкідлива/косметична), `ZVIT-TEKSTY-2.md` (2 000 МАЙ, затверджено).

- **Код проходу** — `src/step1c_teksty.py`, workflow «Прохід по текстах»
  (лише кнопка). Результат — частини `data/teksty/<дата>-NN.csv.gz`.
- **Чекає Андрія:** натиснути «Прохід по текстах» з бюджетом 30 000 —
  МАЙ, НАР, НАС повністю. Після цього — `ZVIT-TEKSTY-3.md` (НАР і НАС, по
  15 фабул, хибні дати); поріг пройдено — вмикається щоденний розклад.
- **Паралельно:** читання частин у `step2_geocode` і `step3_map` (адреса з
  проходу, «20Б → 20», між сусідами, перехрестя, фабула, клас, шкідлива) —
  знімки «було / стане», пуш після перегляду.
- **Уже на сайті (зібрано 23.09 11:13 UTC):** пошук відкриває вікно
  завжди, центрів вулиць на карті немає (19 803 події, 1 111 точок — лише
  через пошук вулиці), номери справ і посилання 2024–2025 у панелі
  (`data/posylannya`), відмінки числівників, порожня картка проблеми
  ховається.

## Крок 5, адреси — закрито

Завдання — `ZAVDANNYA-ADRESY.md`, рішення — розд. 19, діагноз —
`POMYLKA-ADRES.md`. Три коміти **запушено**:

- **068b66e (2б)** — правило вибору статті: таблиця «заявник / виявлено
  поліцією» у `mech.py`, саме правило в `podii.py`, усі статті справи
  зберігаються в події п'ятим елементом. 83 події зі 133 124 мали кілька
  статей, мітка змінилася у 34 (ДОР 32, ГП 2). Чотири пари статей
  лишилися невирішеними — беруть статтю підсумкового рішення.
- **a83669e (3)** — місце події в описі перемагає адресу з номером
  деінде (`addr.py`), NOISE розширено («майданчик», «ГУ НП» — штрафний
  майданчик на Хоткевича, 20-Б ставав місцем крадіжок), перехрестя
  геокодуються посередині між найближчими будинками. Хоткевича 20
  втратила 10 із 15 перевірених чужих подій, Братиславська 3 — 9/15,
  Ревуцького 54 — 6/15. Наявні події перераховуються не тут, а на
  кроці 6, чергою: МАЙ, НАР, НАС → ГП, АЛК, СЕР → ДОР.
- **6f71eb1 (4)** — `addr_class` у даних, «Лише точні адреси» лишає лише
  клас B, дрібне застереження у вікні адреси, знімки
  `site/znimky/5-4-*.png`. Частка класу B: майно 18→43%, наркотики
  33→38%, насильство 30→56%. Наркотики зросли мало через обрізання
  фабули до 600 знаків.

### Рішення за цими комітами (23.09)

1. **Пушити всі три.**
2. **Підпис перехрестя** — «вул. X / вул. Y», без хвоста «· вся вулиця».
3. **Застереження у вікні адреси** — правило «понад 50% показаних подій
   не клас B» приймається з доповненням: рядок показується також тоді,
   коли сама адреса класу C або D, навіть якщо поріг не досягнуто.
   Застереження про чесність даних, а не про більшість.
4. **Картка проблеми, що лишається при «лише точних адресах»** — лишити
   як є, записати в звіт як відому розбіжність; закриється на кроці 8
   (відбір проблем за `NAPRYAM-PROBLEMY.md`, Р1: проблема будується лише
   на класі B).

Далі: дата події (6) → перегрупування видів + одне перенавчання (7) →
відбір проблем (8) → решта MapLibre (9).

## Прийнято 22.09

- `NAPRYAM-PROBLEMY.md` — чинне визначення проблеми (розд. 20): лише
  перевірені адреси, без проактивних статей, повторюваність замість
  порогу 15, ДТП з майновою шкодою окремим шаром, без ранжування за
  шкодою поки що, без квот.

## Чекає на Андрія

- Подивитися знімки `site/znimky/5-4-*.png` — чи влаштовує вигляд
  застереження у вікні адреси.
- ~~«Форма рішення» в Actions~~ — запущено, `0c2073e`. `data/formy.csv.gz`
  повний: 291 209 рядків, форма є для 209 081 з 209 086 подій.
- ~~Перегеокодувати на своїй машині~~ — **не треба, геокешу не існує.**
  `step2_geocode` щоразу робить `DROP TABLE geo` і збирає її наново з
  `events.db` (відновлюється зі знімка `events.csv.gz`) та
  `osm_kyiv_city.json`, який лежить в `actions/cache`; якщо кеш вигорить,
  адресна база перекачається з Overpass просто в Actions. На машині
  Андрія не запускається нічого.
- `PERERAHUVATY.bat` застарів — про це сказано першим рядком у самому
  файлі. Перерахунок адрес робить бот на кроці 6.
- **R** злиття кластерів і ромб на кільці чи окремо (проба
  `site/proba-klasteriv.html`) — знадобиться на кроці 16.
- Перевірка WebGL на комп'ютерах аудиторії — коли `kyiv-gl.html` з'явиться
  на сайті.

## Знайдено 23.09

- **Фабули — найвужче місце.** `data/fabuly.csv.gz` покриває 68 189
  документів із 209 086 (2024 — 24 696 з 78 728, 2025 — 27 756 з 78 585,
  2026 — 15 132 з 48 175). Крок 1b не запускає жоден workflow, і файлу
  немає в переліку збереження `update.yml`; у репозиторій він потрапив
  один раз руками (`f5787e4`, 3 вересня). Фабули тоді качалися саме для
  подій із координатами, тому на карті клас D лише в **1 857 із 59 094**
  подій (збірка як в Actions, 23.09). Прогалина в іншому: фабули обрізано
  до 600 знаків (межа B/C тримається на обрізку), і **76 тис. подій без
  адреси** фабули не мають зовсім. Закриває це прохід кроку 6.
- **Довідник форм рішень** приїжджав із дампу двічі перекодованим
  («Р’РёСЂРѕРє» замість «Вирок»). Полагоджено — `07fd7f0`. Коди звірено з
  першоджерелом: 1 вирок, 2 постанова (саме їх `podii` рахує подією),
  5 ухвала.
- **Звичайний запуск `step0` нічого не стирає:** він читає наявний
  `formy.csv.gz` і лише доповнює його свіжим роком.

## Не встигли — на новий чат (закрито 23.09: `e23ff70`, `3da21b0`)

Рішення 2 і 3 з переліку вище тоді ще не були в коді:

- підпис перехрестя «вул. X / вул. Y» без хвоста «· вся вулиця»
  (`step3_map.py`, блок «чесна назва точки»). Поки таких подій немає
  взагалі — рівень `cross` з'явиться лише з новими текстами;
- застереження у вікні адреси зараз показується тільки за порогом
  «понад 50% показаних подій не клас B» (`tpl_core.py`, `anote`);
  доповнення «показувати й тоді, коли сама адреса класу C або D» не
  зроблено.

## Помічено, не вирішено

- Коміти з повідомленням «1» забирають незакомічені правки виконавця.
- `CLAUDE.md` називає карту «комерційним продуктом» — суперечить рішенню.

## Живі дані

`site/kyiv.html` на машині Андрія (у `.gitignore`). Подія в `P` —
`[суд, стаття, рік, година, клас адреси]` (з `6f71eb1`), а далі, коли у
справі є інші статті, — ще й вони (з `068b66e`).
Після кроку 5 числа зміняться — стара база шести станів більше не еталон.
Еталонні адреси для перевірок — лише з підтвердженим класом B.

## Технічні обмеження середовища

- Прямий `git push` із контейнера заблоковано — пуші через конектор GitHub.
- Overpass API з контейнера недоступний — перевірки OSM лише на машині Андрія.
