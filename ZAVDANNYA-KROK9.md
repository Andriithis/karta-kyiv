# Завдання: крок 9, нова карта на MapLibre, вигляд подій

Складено в центральному чаті 25.09.2026. Макет «Київ від міста до події»
(v14) Андрій затвердив 25.09.2026. Рішення записано в `RISHENNYA.md`,
розділи 6, 16, 18, 21 і 23. Паритет із нинішньою картою описано в
`ZAVDANNYA-MAPLIBRE.md` разом із доповненням від 17.09. Якщо цей файл
розходиться з ними, правильні цей файл і розділ 23.

Макет лежить поруч: `MAKET-KROK9.html`. Він відкривається подвійним
клацанням. Механіку (злиття кілець, розпад при зумі, переходи, перемикачі)
бери з нього. Він зроблений на MapLibre 4.7.1, а збірка — на 6.10.0
(`tpl_gl.py`). Лишаємо 6.10.0.

---

## 0. Перед початком

1. Закомітити `ZAVDANNYA-KROK9.md` і `MAKET-KROK9.html`. У `RISHENNYA.md`
   на початку розділу 23, після абзацу «Макет: …», додати рядок:
   `**Затверджено Андрієм 25.09.2026 за макетом v14 (MAKET-KROK9.html).**`
   Повідомлення коміту: «Крок 9: завдання й затверджений макет вигляду подій».
2. **Надіслати план і не писати код, доки план не затвердять.** У плані:
   - що вже є в `tpl_gl.py` (каркас `0c234f5`) і що лишилося з
     `ZAVDANNYA-MAPLIBRE.md`, пп. 3–5;
   - як при збірці рахується дерево кластерів (п. 3 нижче) і на скільки
     від нього зросте `kyiv-gl.html` (у КБ);
   - чим малюються кільця: шари GL чи власний шар (custom layer), і чому
     саме так;
   - поділ роботи на коміти.

## 1. Межі кроку

- Усе робиться лише в `kyiv-gl.html`. Типовою лишається `kyiv.html`
  (Leaflet) без змін, доки Андрій не затвердить нову після перевірки на
  комп'ютерах і проєкторі аудиторії (розд. 23, «Відкрите»).
- `data/` не чіпати, модель не перенавчати. Малювання від даних не
  залежить, тож крок іде паралельно з кроками 7–8 «Порядку».
- **Види.** У розд. 18 після кроку 7 видів шість («Середовище» зникає),
  у розд. 23 кольорів сім. Для нової карти палітра розд. 23 заміняє
  таблицю розд. 18. Колір прив'язати до **назви виду**, а не до номера в
  переліку. Тоді після кроку 7 сьомий колір (`#DD7DA2`) просто
  відпадає, а решта не змінюються.
- Відкрите питання розд. 21 (вулиця з багатьма подіями без номера
  будинку — лінією) тут не робимо.

## 2. Два вигляди: «Кільця» · «Адреси»

Перемикач угорі картки, як у макеті. Вибір пам'ятається в браузері.

- **Кільця.** Кільце з прозорою діркою 0,62 r, дуги за видами, у центрі
  кількість подій (формат `1,2k`). R = 48 px. Кільця, що налазять одне на
  одне, зливаються, доки перекриттів не лишиться. З z15 MapLibre (= z16
  Leaflet) кілець немає, лишаються адреси.
- **Адреси.** Як на нинішньому сайті: одна крапка на адресу, колір виду,
  якого там найбільше, розмір за правилом кроку 3 (таблиця в
  `ZAVDANNYA-MAPLIBRE.md`, п. 4), виразом `interpolate` за зумом.
- Фільтри (види, період, район) міняють обидва вигляди однаково. Число
  в центрі кільця дорівнює сумі подій його адрес за поточним фільтром.

## 3. Дерево кластерів — при збірці

- `step3_map.py` рахує кластери з кроком 0,5 зуму від міського огляду
  до z15 і записує дерево «хто з кого складається»: для кожного кластера
  є батько й центр. Групування йде знизу вгору: кожен рівень групує
  вузли попереднього, дрібнішого, тож кожен кластер цілком лежить в
  одному батьківському.
- Сум за видами в дереві немає (рішення 25.09): період і статті з них
  однаково не вивести, тож браузер складає суми з адрес за поточним
  фільтром, знизу вгору за батьками. Це ~230 КБ економії.
- Браузер нічого не групує наново, лише анімує переходи між рівнями.
  Коли фільтр змінився, суми перераховуються за деревом, а склад
  кластерів лишається тим самим.
- Кільце, де за фільтром лишилася одна адреса, — звичайна крапка на її
  місці, така сама, як у вигляді «Адреси» (рішення 25.09).
- Коментар у коді має пояснити, чому дерево рахується при збірці: інакше
  анімація «розпад на дочірні» неможлива, бо браузер не знає, хто чий
  нащадок.

## 4. Проблеми — окремий шар поверх обох виглядів

- У «Кільцях» проблема — ромб на краю кільця. Кілька проблем дають
  один ромб із числом. Адреси з проблемами в кластер не ховаються
  (розд. 6).
- В «Адресах» — ромб сталого невеликого розміру (не менше 7 px) з
  контуром кольору чорнила й просвітом.
- Кнопка «◆ Проблеми» вмикає й вимикає ромби в обох виглядах.

## 5. Палітри

- Три палітри: **Яскрава · Крейдяна · Графітова**. Перемикач стоїть у
  «Розширено». Вибір пам'ятається в браузері, типова — Яскрава.
  Непрозорість ~86%, одна палітра і для кілець, і для крапок. Карта біла.
- Значення для світлої теми — з розд. 23, п. 4, у тому самому порядку.
- **Дальтонізм.** Для кожної палітри рахувати найменшу ΔE (CAM02-UCS) між
  будь-якими двома видами за дейтеранопії, протанопії й тританопії.
  Крейдяна (5,4) і Графітова (6,5) мають вийти не нижче за Яскраву (6,8).
  Міняти дозволено лише світлоту й насиченість, відтінок не чіпати.
  Дорожній рух лишається зеленим. Нові значення з числами ΔE —
  **спершу на затвердження**, потім у код.
- **Темна тема.** Окремі значення за тим самим правилом. Теж спершу на
  затвердження, з макетом «було / стане».

## 6. Плавність

- Зум безперервний.
- При наближенні кільце розпадається: дочірні виходять із батьківського
  й розходяться на свої місця. При віддаленні вони збігаються назад.
  Перетікання буває лише під час зуму. Під час перетягування на карті
  один набір кілець, без задвоєння.
- Шар малювання синхронний із кадром карти: кільця не відстають від
  підкладки й не «пливуть».
- Якщо на міському огляді гальмує, написати, де саме і з якими числами
  (кадрів за секунду). Тоді малювати кільця через WebGL. Без WebGL —
  перехід на `kyiv.html` (уже є в каркасі).

## 7. Переходи при натисканні

- Кільце: переліт до його адрес (`fitBounds`), не глибше ніж на 2,5 кроку
  зуму від поточного.
- Адреса: карта м'яко зсувається так, щоб адреса не опинилася під
  карткою, без різкого наближення. Вікно адреси — з `tpl_popup.py`, як
  зараз.

## 8. Решта паритету

Усе з `ZAVDANNYA-MAPLIBRE.md`, пп. 3–5 і доповнення від 17.09, що ще не
зроблено: підписи українською, номери будинків, без `poi`, ризик і потоки
смугами, теплова, райони (маска, межі, підсвітка), вікна й панель рішень,
постійний рядок «Адреса — з рішення суду». Якщо щось у GL не виходить
рівноцінним, зупинитися й написати, що саме і які є варіанти.

## 9. Перевірка очима

Зібрати локально (`py -3 src\step3_map.py`, `PODYVYTYSYA.bat`), відкрити
`kyiv.html` і `kyiv-gl.html` поруч, світла тема й темна:

1. Міський огляд → район → адреса, туди й назад: кільця розпадаються й
   збігаються без стрибків і блимання. Перетягування без задвоєння.
2. Зуми 11, 13, 15, 17: немає кілець, що налазять; з z15 кілець немає.
3. Число в кільці = сума подій його адрес (перевірити з консолі на 3–4
   кільцях, зокрема з увімкненим фільтром одного виду).
4. «Кільця» ↔ «Адреси», «◆ Проблеми» в обох виглядах. Кількість проблем
   на міському огляді й у районі збігається з `kyiv.html` на тих самих
   даних (не з числом 44 у `ZAVDANNYA-MAPLIBRE.md` — воно з часу до
   23.09, коли ще були центри вулиць). У звіті — обидва числа і що саме
   вони рахують: проблеми чи адреси.
5. Кольори на адресах еталона (нижче) — у трьох палітрах і двох темах.
   Вибір палітри й вигляду пам'ятається після перезавантаження.

   **Еталон кольорів (26.09.2026, дані 26.09, «лише точний будинок»).** По
   одній адресі на вид, де цей вид переважає строго, не проблема. Перевірка
   з консолі: `kartaAdresa('Маричанська, 4')` → колір і вид.

   | Вид | Адреса | Подій виду / усього |
   |---|---|---|
   | Порядок | вул. Маричанська, 4 | 16 / 16 |
   | Торгівля | вул. Б. Гаврилишина, 4 | 12 / 12 |
   | Наркотики | вул. Ревуцького, 56 | 11 / 11 |
   | Насильство | просп. Червоної Калини, 37-А | 1 / 1 |
   | Майно | просп. Берестейський, 94/1 | 5 / 12 |
   | Дорожній рух | шосе Берестейське, 8А | 73 / 73 |
   | Середовище | вул. Я. Корчака, 23/2 | 2 / 3 |

   Адрес із ≥10 подіями, де переважає насильство, майно чи середовище,
   на карті немає; тут — найсильніші наявні. Володимирська більше не
   майнова проблема (там переважає дорожній рух), тому з еталона вийшла.
   26.09 усі сім кольорів у трьох палітрах і двох темах збіглися з
   таблицею розд. 23, п. 4.
6. Клік по кільцю — не глибше ніж на 2,5 зуму. Клік по адресі — зсув без
   наближення, вікно, «Усі рішення (N)».
7. Хрещатик і Богдана Хмельницького: смуги ризику поруч, без перекриття.
8. Частота кадрів на міському огляді, у числах, — і на слабкому
   ноутбуці або з уповільненням процесора ×4 у DevTools: карта піде на
   проєктор академії. Полотно кілець — з урахуванням `devicePixelRatio`,
   кільця й числа різкі.
9. Жодної помилки в консолі. Ширина телефону не ламає ні карту, ні
   картку.

## 10. Коміти й звіт

- Кожна логічна частина — окремий коміт українською: перший рядок — що
  змінилося, далі абзац — чому. Коментарі пояснюють чому. Чужі коментарі
  з рішеннями переносити разом із кодом.
- Звіт містить: що зроблено; перелік із п. 9 з позначкою біля кожного
  пункту; знімки обох версій на тих самих місцях (місто, район, адреса);
  ΔE трьох палітр; розмір `kyiv-gl.html` до й після; що вийшло не
  рівноцінним.
