# Що доробити далі

Робочий перелік станом на **1 вересня 2026**.

> **Новий чат: читайте спершу `CILI.md`, потім цей файл.**
> Стан методики — `NAUKA.md`. `STAN.md` глибший за технікою, але написаний
> 31.08 і місцями застарів — див. розділ 2.

---

## 1. Головне зараз

**Довести до ладу те, що вже є.** Вигляд, клікабельність, оновлення, дрібні баги.
Щоб більше не повертатися до цього коду.

Нових можливостей поки не додаємо, крім трьох, перелічених у розділі 4.

---

## 2. Що змінилося 1 вересня

| Зміна | Файл |
|---|---|
| Перерахунок адрес: судових адрес було ~23 900, стало 2 024 | `data/` |
| `TOPN = 25` замість 1200; `TOPGRID = 200` лишається в пам'яті | `step4_engine.py` |
| `MIN_PAI = 3.0` замість фільтра «краще за тему»; шарів 17 замість 7 | `step4_engine.py` |
| Число «збулося»: зі скількох показаних вулиць події потім сталися | `step4_engine.py` |
| Центр вулиці більше не видає себе за будинок і не йде в проблеми | `step3_map.py` |
| Плитки OpenStreetMap замість CARTO, темні через CSS-фільтр | `step3_tpl.py` |
| Підсвітка «Що поруч» гасне при закритті вікна | `step3_tpl.py` |
| Крок 0 не валить прогін, коли портал лежить | `step0_fetch_dump.py` |

**Застаріло в `STAN.md`:** розділ 3 (кількість шарів), розділ 7 (фільтр «краще за
тему» більше не діє), розділ 11 напрям А (порядок інший).

**Важливо:** останній прогін конвеєра завершився о 10:53, а правки карти пішли о
10:56 і 11:03. Тобто на сайті ще **немає** плиток OSM, гасіння підсвітки й чесних
назв точок. Достатньо запустити Run workflow **без галочок**.

---

## 3. Числа, на які спиратися

Дані за 2024 — серпень 2026, адреси установ виключено.

- **152 проблеми на 129 адресах** — 0,37% адрес міста
- **126 зі 152 тримаються всі три роки**
- 12 адрес мають кілька проблем одночасно
- дві третини адрес міста дали за три роки рівно одну подію
- проблеми стоять на позначених моделлю вулицях у **27%** випадків проти **17%**
  у звичайних подій — тобто ×1,6; у числах 41 проблема зі 152
- якість моделі: PAI 3,5—5,9; перенесення на іншу половину міста 30—43%

Проміжний звіт опубліковано окремою сторінкою — артефакт «Проблемні місця Києва».

---

## 4. Завдання

### 4.1. Наведення ладу `[головне]`

Не окреме завдання, а режим роботи: пройти карту від початку до кінця, знайти й
виправити все, що заважає.

Відомі баги:

- **картка проблеми на районній карті залазить угору**, її не видно;
- дрібниці, які виявляться при уважному проході.

Після кожної правки — перебудова карти на синтетичному місті, перевірка балансу
тегів і `node --check`. Не покладатися на те, що «має працювати».

### 4.2. Один файл замість одинадцяти

Зараз збираються місто плюс десять районів, окремими файлами по ~12 МБ. Перехід
між ними — перезавантаження сторінки.

**Робимо один файл.** Міська карта вже несе всі дані; районні — підмножини, і
вхід у сайт усе одно через міську. Клік по району: карта переїжджає, фіксується,
затемнює решту.

**І головне — район стає рівнем аналізу, а не окремим файлом.** Переліки проблем
і ризиків перераховуються всередині району зі своїми порогами. Місце з вісьмома
епізодів не тягне на міську проблему, але може бути найгіршим у Деснянському.
Дільничний працює районом, а не містом.

Заразом зникає баг 4.1, бо зникає окрема гілка збірки для районів.

Перед впровадженням — виміряти вагу об'єднаного файлу.

### 4.3. Фабули за посиланням із картки проблеми

Без виїздів **текст рішення — єдине джерело причини**.

`step1_download.py` качає текст, витягує адресу і **текст викидає** — у базі його
немає. Але повний текст доступний за посиланням на реєстр, і `doc_url` у нас є.

Робимо: у картці проблеми — перелік посилань на рішення саме цієї проблеми
(адреса + механізм), а не на адресу загалом. Щоб слухач відкрив і прочитав.

### 4.4. Значки замість кружечків

Чинники середовища зараз — однакові кружечки різного кольору. Треба **літери**:
М — метро, Ш — школа, Р — ринок, А — АЗС, Б — бар.

`step3_tpl.py`, функції `drawFacts`, `showNear`, `showAllNear`. Обережно: карта
малюється з `preferCanvas: true`, `divIcon` піде в DOM-шар — на 5 000 об'єктів
може просісти. Обмеження за масштабом уже є (`FZOOM = 14`).

### 4.5. Правові назви статей

У листі до балансоутримувача стаття має бути названа юридично точно. `labels.py`
машинно-згенерований — регенерувати з правильними назвами.

### 4.6. Картка проблеми до рівня звіту

Роки повторення, розклад доби, чи це одна з 12 адрес із кількома проблемами, чи
пояснює середовище. Дані вже пораховані, треба донести до `pblock` у `bindPopup`.

### 4.7. Дедуплікація в `step3_map.py` `[борг]`

«Одна справа = одна подія» є лише у двигуні. На карті проблеми рахуються за
документами, тому кримінальні скупчення перебільшені: одна крадіжка з чотирма
ухвалами важить як чотири.

**Це змінить список зі 152 проблем** — імовірно, поменшає в майнових і
наркотичних механізмах.

### 4.8. Перевірити нові джерела даних

Список і що в кожному шукати — у `NAUKA.md`, розділ 6. Коротко: офіційні ДТП
Нацполіції, відкриті дані Києва, звернення 1551, GTFS Київпастрансу, Prozorro,
Mapillary.

Завдання на цьому етапі — **не впроваджувати, а з'ясувати**, що там лежить, у
якому форматі й чи оновлюється. Доповісти результат.

### 4.9. Перевести модель на протокол RTM

Див. `NAUKA.md`, розділ 3. Перший крок — записати список чинників із
обґрунтуванням **до** прогону, а не після.

### 4.10. Специфічність ризиків

З 425 рядків на карті унікальних відрізків лише **137**; Оболонський проспект
трапляється в 15 шарах із 17. Великий проспект набирає високу оцінку для всього
одразу.

Ранжувати за специфічністю: оцінка механізму, поділена на середню оцінку всіх
шарів для цього самого відрізка.

`step4_engine.py`, побудова `rk` перед `ordr = np.argsort(-rk)`. Потрібен другий
прохід. Тема й механізм не повинні ділитися одна на одну.

Після зміни виміряти: унікальних відрізків серед 425 (зараз 137) і частку проблем
на позначених вулицях (зараз 27%, не має впасти).

### 4.11. ДТП з потерпілими — рішення відкладене

Не набирає `MIN_EV = 250` навіть на двох роках. Проблем цього механізму лише 3.

Варіанти: дозволити події рівня «вулиця» (точність падає до вулиці) або опустити
поріг до ~150 (модель нестабільна).

**Можливо, питання зникне саме**, якщо знайдуться офіційні дані ДТП із
координатами (4.8). Тому рішення відкладено до перевірки джерел.

---

## 5. Правила роботи з репозиторієм

**Запис у GitHub працює.** Через конектор — `mcp__GitHub__push_files` із повним
вмістом файлу. Обговорюємо в чаті, зміни вносяться одразу.

Не працює лише команда `git push` — її блокує проксі. Це не обмеження проєкту.

**Єдиний реальний виняток:** токен не має дозволу `workflow`, тому
`.github/workflows/update.yml` редагує лише користувач вручну. Будь-яке
виправлення конвеєра має вміщатися в `src/`.

**Після кожного push — перевірка.** Символ `і` (U+0456) зрідка перетворюється на
інший схожий. Обов'язково:

```
git clone --depth 1 https://github.com/Andriithis/karta-kyiv.git /tmp/chk
diff локальний_файл /tmp/chk/src/файл
python3 -m py_compile /tmp/chk/src/*.py
```

Для `step3_tpl.py` додатково: витягнути `<script>` зі згенерованої `karta.html`,
перевірити `node --check`, розмітку — балансом тегів через `HTMLParser`.

**Мережа з пісочниці.** `reyestr.court.gov.ua`, `overpass-api.de` та
`api.github.com` через curl недоступні. Працюють WebSearch, WebFetch, конектор
GitHub і `git clone`. Двигун і карту тестуємо на синтетичному місті — воно
збирається з реального набору подій.

**Мова.** Українською. Пункти інтерфейсу називати їхніми українськими підписами.

**Тон.** Коротко й простими словами.

---

## 6. Журнал рішень 1 вересня

- **Прибрано фільтр «механізм має бути точнішим за тему».** Різниця 38,0% проти
  38,4% — шум, а карта через нього лишалася без шару «ДТП». Замінено порогом
  `MIN_PAI = 3.0`.
- **1200 вулиць у шарі скорочено до 25.** У хвості події були лише на 26% вулиць.
- **Центр вулиці не вважається місцем.** Коли будинку немає в OSM, подія стає в
  центр вулиці; такі купи підписувалися чужим номером будинку.
- **Звіт перебудовано навколо проблем, а не ризиків.** Проблема — мета, ризик —
  інструмент.
- **Карта дефектів визнана правильним напрямом для ризиків**, а OSM — непридатним
  джерелом для неї. Див. `NAUKA.md`, розділ 5.
- **Райони стають рівнем аналізу, а не окремими файлами.**
- **Застосунок із підпискою відхилено.** Причини — у `CILI.md`.

---

*Оновлено 1 вересня 2026.*
