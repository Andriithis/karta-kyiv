# План: власна база подій замість обробки на льоту

24.09.2026. Лише план, коду немає. На перегляд центральному чату.

База — лише для цього навчального проєкту. «Моя вулиця» — окремий проєкт
Андрія: код і дані звідси туди не переносяться.

## Що зараз і чому це болить

Подія на карті збирається щоразу наново з кількох джерел:
- `events.csv.gz` (документи з кроку 1);
- `formy.csv.gz` (вирок / постанова / ухвала);
- `posylannya/` (номер справи й посилання);
- частини проходу `data/teksty/*.csv.gz` (адреса, фабула, дата, клас);
- `step2_geocode` (точка);
- `map_excl` (установи);
- `podii.merge_cases` (одна подія на справу).

Кожен споживач — карта, звіти, модель — повторює цей ланцюг сам. Звіти
розійшлися з картою в 2,3 раза (142 тис. проти 62 тис., виправлено
24.09), модель досі вчиться на документах.

Повний текст рішення прохід качає і викидає. Нове правило розбору (`v2`,
`v3`) перераховується лише на збереженій фабулі, обрізаній до 1 000 /
2 500 знаків: якщо місце чи дата стоять далі, їх уже не взяти, а старий
результат лишається як є (`pererakhuvaty`).

## Що пропонується

1. **Одна таблиця подій** `data/podii.csv.gz` — рядок на подію (справа ×
   вид), з неї читають карта, звіти й модель.
2. **Повні тексти** — стиснутими архівами в GitHub Releases репозиторію:
   нове правило перераховується з повного тексту без повторного
   завантаження з реєстру.
3. **Щотижневе докачування:** нові рішення з дампу → текст в архів тижня →
   розбір → рядки в таблицю.

### Таблиця подій (поля)

| група | поля |
|---|---|
| хто / що | `podia` (справа + вид), `sprava` (номер), `vyd`, `stattya` (підпис), `statti` (інші статті справи), `kodeks` |
| документи | `doc` (представник), `docs` (усі папери справи з посиланнями), `forma` |
| коли | `data_podii`, `chas_podii`, `data_rishennya`, `sud` |
| де | `vulytsya`, `budynok`, `riven` (house / cross / street / hidden / none), `klas` (B / C / D), `lat`, `lon`, `tochnist` (house / base / interp / cross / street), `ustanova` (так / ні) |
| опис | `fabula` (зі стелею), `vada` (шкідлива / косметична) |
| походження | `pravylo` (`v3`…), `adresa_rechennya`, `onovleno` |

Розмір: ~133 тис. рядків; разом з фабулою ~25–30 МБ у gzip. Це як дві
частини проходу, тобто в репозиторії, як і зараз. Установи й точність —
полями, а не вилученням: карта й звіти відсіюють однаково, а скільки
відсіяно — видно.

### Повні тексти

Виміряно 24.09 на 40 випадкових рішеннях (25 КУпАП, 15 КК):

| | КУпАП | КК |
|---|---:|---:|
| RTF, медіана | 18 КБ | 40 КБ (середнє 81) |
| текст, знаків (середнє) | ~6 000 | ~25 000 |
| текст у gzip, по одному | 2,9 КБ | 7,4 КБ |
| подій | 120 123 | 13 494 |
| разом у gzip, по одному | ~340 МБ | ~100 МБ |

- **~440 МБ**, якщо стискати кожен текст окремо. Пачкою в один архів
  (`jsonl` → `xz`) очікую 150–250 МБ: слова спільні. Точне число — з
  першого архіву.
- Нестиснені тексти — близько 2 ГБ; у git їм не місце.
- **Архіви по місяцях дати рішення:** `teksty-2025-03.jsonl.xz`, ~5–10 МБ
  кожен. Ліміт GitHub — 2 ГБ на файл, запас великий.
- Старі місяці не змінюються; новий тиждень дописується в архів свого
  місяця (у Release файл замінюється цілком).
- Ухвали й інші папери справи (ще ~75 тис. документів) — окремим рішенням.
  Для подій вони не потрібні, для «Усі рішення» досить посилання.

### Час

| що | скільки |
|---|---|
| одноразово докачати тексти (Actions, ~2,2 док./с, як бот зараз) | ~17 год — 3–4 запуски по 6 год; на машині Андрія ~6 док./с — ~6 год |
| перерахунок нового правила з текстів | оцінка 30–60 хв в Actions (зберегти 30 000 фабул — 1–2 хв; повний розбір з пошуком фабули довший) — заміряти на першому архіві |
| тиждень нових рішень (~900: 32,6 тис. рішень по суті за 38 тижнів 2026) | хвилини |
| збірка карти з готової таблиці | швидше за теперішню: без злиття справ і перевірок на льоту |

Економія видна вже зараз. Бот щодня качає по 30 000 текстів ДОР і
викидає їх; якщо до кінця ДОР почати їх зберігати, не доведеться
перекачувати ~100 тис. вдруге. **Тому перший крок — зберігання текстів у
проході, ще до таблиці.**

## Як перенести `data/teksty/`

1. Частини лишаються історією. Вони не видаляються і нікуди не
   переписуються.
2. Таблиця подій уперше збирається з того самого ланцюга, що й карта
   (`step3_map.vybir()`, з 24.09 він один для карти й звітів), плюс частини
   з чинним правилом. Перевірка: карта, зібрана з таблиці, дає ті самі
   61 989 подій і ті самі точки, що зібрана по-старому.
3. Тексти першим архівом:
   - документи, які бот обробить після зміни, — одразу в архів;
   - ~33 тис. уже розібраних (частини 23–24.09) — докачати окремим
     запуском (~4 год).
4. Далі нове правило (`v4`) читає тексти з архівів, а не з реєстру. Прохід
   з бюджетом 30 000 на день потрібен лише для докачування нового.

## Порядок

1. Прохід зберігає повний текст (`jsonl.xz`) і кладе архів у Release
   `teksty` (`gh release upload`, GITHUB_TOKEN з `contents: write`).
2. Докачати тексти вже розібраних документів.
3. `data/podii.csv.gz` і `step3_map` читає її (звірка з теперішньою
   картою: події, точки, класи).
4. Звіти (`step6_base`) і модель (`step4_engine`) читають ту саму таблицю.
   Модель — разом із кроком 7, бо це й так перенавчання.
5. Щотижневе докачування в «Оновленні карти» замість щоденного бота.

## Відкриті питання

1. **Чи публікувати повні тексти в Release публічного репозиторію.**
   Тексти й так відкриті в ЄДРСР і знеособлені (ОСОБА_N, АДРЕСА_N). Але це
   друга публікація цілого масиву, і її вже не відкликати. Варіанти:
   - публічний Release;
   - приватне сховище (окремий приватний репозиторій, Actions тягне
     токеном);
   - тримати тексти лише в кеші Actions (до 10 ГБ, але вигорає через 7 днів
     без звернень, тож ненадійно).
2. **Геокодування** — у таблиці (поля `lat`, `lon`) чи окремо. Якщо в
   таблиці, оновлення бази OSM змінює всю таблицю. Пропоную:
   - адреса з тексту — у таблиці;
   - точка — окремим файлом `data/tochky.csv.gz` (адреса → точка);
   - обидва перераховуються незалежно.
3. **Установи** — позначка в таблиці чи вилучення. Пропоную позначку:
   перелік установ змінюється частіше за тексти.
4. **Що вважати подією, коли справа має кілька видів** (ДТП + ст. 130).
   Зараз — одна подія на (справа, вид). Лишаємо, записати в таблиці явно.
5. **Хто оновлює:** щотижневий запуск «Оновлення карти» чи окремий
   workflow бази. Одночасний запуск з ботом уже раз відхилив пуш —
   потрібна одна черга (`concurrency` уже спільна: `karta-update`).
6. **Скільки тримати в репозиторії.** Таблиця ~30 МБ і частини проходу по
   ~8 МБ — у git. Кожне оновлення таблиці додає до історії ще ~30 МБ, за
   рік — гігабайти. Варіанти:
   - тримати таблицю в Release так само;
   - ділити таблицю по роках, щоб оновлювався лише поточний.
