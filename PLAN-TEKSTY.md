# План кроку 6 — прохід по текстах рішень

До `ZAVDANNYA-TEKSTY.md`. 23.09.2026. Коду ще немає, це план на затвердження.

Числа нижче — зі збірки «як в Actions» (база зі знімка `events.csv.gz`,
`formy.csv.gz`, `fabuly.csv.gz`, без локальної таблиці `fab`) і з вибірки
140 текстів (по 20 на вид), скачаних 23.09 з темпом кроку 1.

## 1. Що є зараз

| | документів | з них ухвал | подій (вирок, постанова) | з координатами | з фабулою |
|---|---:|---:|---:|---:|---:|
| ДОР | 118 015 | 4 272 | 113 743 | 51 550 | 50 024 |
| ГП | 13 005 | 1 611 | 11 394 | 5 572 | 5 408 |
| НАР | 27 895 | 19 003 | 8 892 | 4 666 | 4 494 |
| МАЙ | 41 145 | 37 239 | 3 906 | 2 062 | 2 010 |
| АЛК | 3 211 | 27 | 3 184 | 1 543 | 1 472 |
| НАС | 5 332 | 4 426 | 906 | 351 | 343 |
| СЕР | 483 | 202 | 281 | 108 | 106 |
| **разом** | **209 086** | **66 780** | **142 306** | **65 852** | **63 857** |

Після злиття «одна справа — одна подія» подій ~133 тис.

**Уточнення до `STAN-CHATU.md` («більшість подій матиме клас D»).** Ні:
фабули колись качалися саме для подій із координатами, тож на карті клас D
має лише 1 857 подій із 59 094 (B 51 184, C 6 053). Прогалина — деінде:
- фабули обрізані до 600 знаків, тому межа B/C тримається на обрізку;
- 76 тис. подій **без координат** (адресу не знайдено старим `extract()`)
  фабули не мають зовсім, і на карту вони не потрапляють;
- адреси майже всіх подій витягнуто ще до правил a83669e.

## 2. Що бере прохід з одного тексту

**Фабула.** Пропоную не писати межі з нуля, а розширити чинний
`step1b_fabula.excerpt()` (у ньому вже є STOP, TAIL, межа речення,
відкидання процедурного вступу) і прибрати стелю 600:

- початок — `A.body_start()` («ВСТАНОВИВ»), для постанов КУпАП без нього —
  «згідно (з) протоколу…». На вибірці знайдено дві вади, які треба
  виправити: `body_start` іноді ловить «встановив» глибоко в тексті (вирок
  починався з резолютивної частини), а кримінальні вироки мають розділ
  «Історія провадження» перед обставинами;
- кінець — чинні STOP/TAIL плюс маркери із завдання («таким чином», «вину
  визнав/не визнав», «дослідивши матеріали») і знайдені на вибірці:
  «визнається винним», «Згідно з ч. 2 ст. 7 КУпАП», «Відповідно до
  ст. 268», «З аналізу ст.» — це шаблонні абзаци постанов, що йдуть одразу
  після фабули.

Виміри на 140 текстах (повний текст — медіана 6 395 знаків):

| | медіана | p75 | p90 | p95 |
|---|---:|---:|---:|---:|
| лише маркери із завдання | 1 418 | 2 950 | 4 775 | 7 368 |
| чинний витяг без стелі + маркери | **619** | 1 444 | 2 580 | 3 884 |

За видами (медіана / p90): ДОР 433 / 1 217, ГП 365 / 1 966, АЛК 280 / 1 172,
НАР 933 / 3 163, НАС 1 370 / 4 903, МАЙ 1 909 / 5 471. Довгі — це кримінальні
вироки, де фабула справді довга (кілька епізодів, опис майна).

**Стеля — пропоную 1 500 знаків, обрізання лише по межі речення.** Довших
за 1 500 — 33 зі 140, здебільшого МАЙ і НАС. Альтернатива — 1 500 для
КУпАП і 2 500 для КК; рішення за вами.

**Дата й час події** — з фабули: дата є у 138 зі 140, час — у 125.
Береться перша дата-час, що стоїть до місця події (`addr.find_time` уже
вміє час).

**Речення адреси** — те, з якого `extract()` узяв місце (у нього вже є
`sentence_end`); зберігається, щоб у панелі й при ручній перевірці було
видно, звідки адреса.

**Адреса й клас** — чинний `extract()` і `podii.addr_class()` на повній
фабулі.

## 3. Як влаштовано

- Новий `src/step1c_teksty.py` і workflow «Прохід по текстах»
  (`teksty.yml`): щодня за розкладом плюс кнопка, `concurrency: karta-update`.
- Черга: МАЙ, НАР, НАС → ГП, АЛК, СЕР → ДОР; усередині виду — від
  найгустіших адрес. Лише представники справ (вирок, постанова), ухвали не
  качаються.
- Темп — як у кроці 1: 4 потоки, пауза 0,12 с (константи беруться з
  `step1_download`, не копіюються).
- Бюджет — 30 000 документів на запуск; позиція в черзі — те, що вже
  записано в результатах, тож наступний запуск продовжує сам.
- **Результат пишеться окремо від `events.csv.gz`**: той файл пише й
  відновлює крок 1 з фіксованим переліком колонок. Прохід пише свої рядки
  `doc_id, фабула, дата, час, речення адреси, вулиця, будинок, рівень, клас,
  правило`, а `step2_geocode` бере адресу з проходу, якщо вона там є.
- Після того як черга пройде, `update.yml` запускає той самий код для нових
  документів тижня — окремим кроком після кроку 1.

**Посилання на тексти в Actions — окрема перешкода.** `kyiv_*.csv` не
зберігаються між запусками: крок 0 качає дамп лише поточного року (минулий —
у перший тиждень місяця). Посилань на рішення 2024–2025 у бота немає; за кодом
`step3_map` (словник `extra`) і панель на живому сайті для них без посилань
та номерів справ — на сайті це ще не перевірено. Пропоную так: перший запуск
проходу раз качає дампи 2024 і 2025 кроком 0 і кладе в репозиторій
`data/posylannya.csv.gz` (doc_id → 34 знаки посилання, лише події; ~3 МБ).
Машина Андрія не потрібна.

## 4. Числа

**Скільки текстів:** ~133 тис. представників справ, включно з 63 857, для
яких фабула вже є. Їх теж треба качати наново: адреса й клас рахуються на
повному тексті, а старі фабули обрізані.

**Скільки днів:** з цієї машини — 19 текстів/с (медіана запиту 0,07 с). У
GitHub Actions буде повільніше; якщо 5/с, то 30 000 — це 1,7 год. **Разом
5 щоденних запусків**, тобто тиждень. Швидкість в Actions покаже перший
запуск.

**Розмір фабул** (UTF-8 1,76 байта на знак, gzip стискає в 4,9 раза):

| стеля | усі ~142 тис. подій | gz |
|---|---:|---:|
| 1 000 | 69 млн знаків | ~24 МБ |
| **1 500** | 79 млн | **~27 МБ** |
| 2 500 | 91 млн | ~31 МБ |
| без стелі | 102 млн | ~35 МБ |

Понад 20 МБ, тому один файл, що переписується щодня, роздув би історію git
на 27 МБ × кожен запуск. **Пропоную частини, що лише додаються:**
`data/teksty/2026-09-24.csv.gz` — у кожному файлі тільки документи цього
запуску (~5 МБ). Старі частини не переписуються, пізніша перемагає. Історія
росте на ~27 МБ один раз, далі — на кілька сотень КБ на тиждень. Перерізати
знову (нове правило) — це нова серія частин, але лише коли змінюється
правило. `fabuly.csv.gz` після переходу стає не потрібен.

**Панель:** `site/spravy/*.json` зараз 51 МБ на 11 файлів (найбільший 6,4 МБ).
За стелі 1 500 фабули на карті зростають з ~29 до ~33 млн знаків (+15%).
Найбільший файл стане ~7,4 МБ; Pages віддає його стиснутим, це ~1,5 МБ на
передачу. Файл тягнеться раз, коли відкривають панель. Сповільнення не
чекаю, але перевірю на телефоні. Окремо: події, які отримають адресу вперше,
додадуть точки й рядки — скільки саме, покаже перший запуск.

## 5. Порядок роботи

1. Код витягу + 20 прикладів фабул різних видів і розподіл довжин на
   свіжій вибірці → на затвердження, **до масового запуску**.
2. Workflow і `posylannya.csv.gz`, один запуск на 2 000 документів (МАЙ) —
   звіт про швидкість і зміни адрес.
3. Решта черги.
4. Перезбірка карти, приймання.

## 6. Приймання — числа «до»

Збірка «як в Actions» 23.09 / живий сайт (зібрано 23.09 06:34 UTC):

| адреса | подій зараз | з них B |
|---|---:|---:|
| вул. Братиславська, 3 | 44 / 46 | 15 |
| вул. Ревуцького, 54 | 84 / 84 | 10 |
| вул. Володимирська, 26 | 1 / 1 | — |
| вул. Г. Хоткевича, 20 | окремої точки немає | |

Г. Хоткевича, 20 на карті окремо не видно: будинку немає в OSM, і ~63 події
з «20», «20Б», «20А» стоять у центрі вулиці «вул. Г.Хоткевича · вся вулиця»
(175 подій, B 123). Приймати по ній пропоную за даними, а не за точкою.

## 7. Живий сайт: ухвали і лічильники

**Ухвали відсіяно, зменшення є — але воно зосереджене в темах, яких на
карті мало.** Та сама база, код до коміту 1 (`a8a3499`) проти чинного:

| | до | після | живий сайт |
|---|---:|---:|---:|
| подій на карті | 61 689 | 59 094 | 60 026 |
| Майнові | 3 241 | 1 843 | 1 852 |
| Насильство | 464 | 315 | 316 |
| Наркотики | 4 557 | 4 193 | 4 218 |
| Громадський порядок | 4 804 | 4 711 | 4 873 |
| Дорожній рух | 47 136 | 46 580 | 47 271 |
| вул. Володимирська, 26 | 66 | 1 | 1 |

Чому загалом лише −4%: дорожній рух — 80% карти, а ухвал у ньому мало. І з
66 780 ухвал координати мали лише 4 417 (МАЙ 2 489, НАР 809): у більшості
ухвал адреси немає зовсім, тож на карту вони й раніше не потрапляли; ще
2 065 уже відсіював старий фільтр за текстом. Майнові впали на 43%.

**Лічильники, що досі рахують документи, а не події (справи):**
- **`rezyume.html`** — «подій опрацьовано 142 311»: це рішення по суті без
  злиття справ і разом із подіями без адреси. Там само «адрес з подіями
  36 691» — адреси з тексту до геокодування й до вилучення установ.
- **`analiz.html`** — усі розподіли (години, теми, райони, концентрація)
  рахують рішення, а не справи, і без вилучення установ: у дорожньому русі
  «з 70 552 подій з часом», тоді як на карті їх 47 тис. День тижня й місяць
  рахуються за **датою рішення**, не події.
- Джерело обох — `step6_base.load_events()`: фільтр ухвал є, злиття справ
  (`merge_cases`) і вилучення установ — немає.
- Карта, вікно адреси, плитки `index.html` — рахують події (справи); тут
  розбіжностей не знайшов. Кнопка «Усі рішення (N)» показує число справ, а
  список під нею — усі папери цих справ, тому рядків у списку більше, ніж N.
  Можливо, саме тут видно «незменшення».

У документах зменшення найбільше: до коміту 1 `step6_base` ухвал не
відсіювала зовсім, тобто рахувала всі ~209 тис. документів. Тепер — 142 311.
Виправлення `load_events()` (злиття справ, вилучення установ, дата події) —
окремий дрібний коміт; пропоную зробити його разом із кроком 6, коли
з'явиться дата події.
