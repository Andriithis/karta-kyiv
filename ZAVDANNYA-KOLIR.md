# Завдання новому чату: кольори маркерів і визначення «проблеми»

Складено 15.09.2026 за зауваженнями Андрія до живої карти
(`https://andriithis.github.io/karta-kyiv/kyiv.html`). Це комерційний
продукт — карта має читатися з першого погляду, без пояснень.

**Перед роботою прочитати в проєкті:** `claude/audyt-2026-09-10.md` (попередній
аудит), `claude/vykonano-2026-09-14.md` (що вже полагоджено), `claude/produkt.md`.

---

## Що побачив Андрій

1. Клікнув маркер у переліку проблем — **три події** («просп. Берестейський, 82:
   2 ДТП + 1 сп'яніння»), а внизу підпис «Ця адреса — у списку проблем, але за
   іншим напрямком (Наркотики)». Питання просте: що це за проблема з двома
   правопорушеннями.
2. **Вул. Борщагівська, 154 світиться синім**, а в ній 30 ДТП і 18 крадіжок.
   Синій — колір майнових. Переважає дорожній рух.
3. **Більшість проблем червоні** (червоний = громадський порядок).
4. **Знімаємо галочку «Громадський порядок» — кольори маркерів міняються**
   у половини точок. Червоні стають фіолетовими, помаранчевими, синіми.
5. Відкриваємо **червону** проблему (вул. Володимирська, 26) — а там 26 крадіжок,
   10 грабежів, 6 розбоїв. Це майнові, а не громадський порядок.

Його слова: «тут взагалі якась нерозбериха та хаос».

---

## Діагноз — два корені, а не п'ять помилок

### Корінь A. Колір маркера береться з ПЕРШОЇ події в масиві

`src/tpl_popup.py`, рядок 223:

```js
let n=0,th=null;
for(const e of p[4]) if(C.has(e[0])&&A.has(e[1])&&Y.has(e[2])&&(!H.size||H.has(e[3]))){
  n++; if(th===null) th=CATTH[e[1]]   // <- тема першої ж події, що пройшла фільтр
}
```

і далі рядок 255: `fillColor: p[3] ? PALA[th%PALA.length] : '#5f6878'`.

Тобто колір — це тема випадкової події: тієї, що лежить першою в масиві
`p[4]`. Не переважної, не тієї, за якою адресу відібрано в проблеми. Звідси
**всі чотири симптоми 2–5 одразу**:

- Борщагівська синя, бо першою в масиві лежала крадіжка, хоча ДТП удвічі більше;
- Володимирська червона, бо першим лежало щось із громадського порядку;
- «більшість червоних» — просто наслідок того, в якому порядку події
  потрапили у файл;
- знімаємо тему — міняється те, яка подія «перша», отже міняється колір.
  Це не окремий баг, це той самий.

Симптом 4 особливо шкідливий: користувач бачить, що колір реагує на фільтр,
і робить із цього висновок про дані. Висновок хибний.

### Корінь B. «Проблема» і «те, що зараз показано» живуть окремо

- Режим «Тільки проблеми» (`tpl_popup.py:222`) лишає адресу, якщо вона проблема
  **за будь-яким** напрямком: `if(CF>=0 && !probsOf(p).length) continue;`.
  Фільтр статей при цьому може ховати саме той напрямок. Так у переліку
  проблем опиняється адреса з трьома подіями — симптом 1.
- Число біля адреси в переліку — це кількість подій **за поточним фільтром**
  (`rank` з `vis`), а не кількість однорідних епізодів, за якими адресу
  визнано проблемою (`pr.n`). Тому «96» у переліку і «37 однорідних подій»
  у картці — різні числа про одне й те саме місце.
- Лічильник `#cathint` («У поточних межах: N проблем») рахує проблеми взагалі,
  не зважаючи на фільтр статей, — ще одне число, що не сходиться з тим,
  що видно.

Нагадування, як влаштований відбір (`src/map_problems.py`): проблема — це пара
«адреса + механізм», що подолала `MIN_EPISODES = 15` однорідних епізодів.
Адреса може бути проблемою за кількома напрямками, і це навмисно.

---

## Що зробити

### 1. Колір маркера має бути пояснюваним і стабільним

Правило, погоджене з Андрієм 15.09.2026:

- **Адреса-проблема** → колір напрямку її проблеми; якщо напрямків кілька —
  той, у якого більше епізодів. Колір не міняється від фільтрів, **доки на
  цій крапці за фільтром (статті, рік, час доби) є події цього напрямку**.
  Якщо немає — колір падає на переважну тему, інакше точка світилася б
  схованою темою.
- **Звичайна адреса** → переважна тема серед показаних подій: явний лідер за
  кількістю, не більшість понад 50%. **Нічия — перша тема за порядком
  `M.groups`.** Сірого на нічию немає: сірий уже означає «центр вулиці».
- **У вікні кожної адреси з номером будинку — частка:** «За поточним
  фільтром тут переважає дорожній рух, 34 із 77.» Тема частки рахується
  окремо від кольору. Коли колір — за напрямком проблеми, а переважає інша
  тема, перед цим окреме речення: «Колір — за напрямком проблеми
  (громадський порядок).»
- Рядка-легенди в панелі немає: крок 3 прибирає пояснювальні абзаци.

Перевірка на даних, перезібраних 15.09 (старі числа «30 із 57» і «26+10+6
із 77» застаріли): Борщагівська, 154 — зелена, дорожній рух 34 із 77. Це
44%, не більшість, тож рядок у вікні перевіряє саме правило явного лідера;
колір там іде від напрямку проблеми, теж дорожнього руху (26 епізодів).
Володимирська, 26 — синя, майнові 46 із 66. Зняття «Громадський порядок» не
міняє колір жодної проблеми з іншим напрямком; проблеми за громадським
порядком переходять на переважну тему.

### 2. Перелік проблем має відповідати фільтру

- У режимі «Тільки проблеми» лишати адресу, лише якщо видимий **той самий
  напрямок**, за яким її відібрано (у коді вже є `GVIS` — набір видимих тем,
  рядок 214; він застосовується до карток, але не до відбору).
- Число поруч з адресою **лишається кількістю подій за фільтром**. `pr.n`
  сюди не переносимо: воно не реагує на період, і на кроці 4 читалося б
  як баг.
- Членство в переліку й `#cathint` — за `GVIS`, тобто за фільтром статей,
  **але не за періодом**: обраний рік — це «покажи події цього року», а не
  «адреса перестала бути проблемою». Інакше проблема зникала б у березні й
  поверталася у квітні.
- `#cathint` рахувати по тому самому правилу, що й перелік.
- Коли адреса — проблема за прихованим напрямком, вона не проблема для
  поточного вигляду. Підпис у вікні лишити (він корисний), але з переліку
  й лічильника прибрати.

### 3. Перевірити очима, а не тільки в коді

Зайти на живу карту в браузері, повторити всі п'ять кроків Андрія і
переконатися, що кожен дає осмислений результат. Перевірити і в районному
вигляді — там свій перелік проблем (`chosen_loc`, `DISTRICT_TOP = 10`).

---

## Як перевіряти

Локально, без GitHub:

```
py -3 src\step3_map.py
PODYVYTYSYA.bat
```

Відкрити http://127.0.0.1:8777/ і пройти всі п'ять кроків Андрія з розділу
«Що побачив Андрій». Кожен має дати осмислений результат.

Іменні перевірки:

Числа — на даних, перезібраних 15.09.2026.

- **вул. Борщагівська, 154** — зелена; у вікні «переважає дорожній рух, 34 із 77»;
- **вул. Володимирська, 26** — синя; у вікні «переважає майнові, 46 із 66»;
- **вул. Антоновича, 50** — колір громадського порядку (напрямок проблеми),
  у вікні два речення: переважає дорожній рух, 40 із 84;
- **вул. Вишгородська, 33** — нічия: громадський порядок 8, дорожній рух 8 —
  колір громадського порядку, перша тема за `M.groups`;
- зняти галочку «Громадський порядок» — **колір жодної проблеми з іншим
  напрямком не змінюється**;
- **бульв. Вацлава Гавела, 31**, обрано лише 2026 рік — зелена (2 ДТП), хоча
  проблема за громадським порядком, і в режимі «Тільки проблеми» лишається;
- **вул. Братиславська, 5** (проблема за громадським порядком, 3 ДТП) у режимі
  «Тільки проблеми» з увімкненим лише дорожнім рухом — у переліку її бути не
  повинно. Колишньої перевірки «просп. Берестейський, 82» у даних більше
  немає: там тепер 82В без проблем.

Перевірити і в районному вигляді: там свій перелік проблем
(`chosen_loc`, `DISTRICT_TOP = 10`).

Решта правил роботи — у `CLAUDE.md`.

---

## Ще не все

Андрій казав, що додасть до цього переліку ще задач. Тому цей документ —
початок, а не повний обсяг роботи. Перш ніж братися, спитати в нього, що ще
має увійти в цю чергу.
