# Як працювати в цьому репозиторії

Карта правопорушень Києва за даними ЄДРСР. Інструмент для курсу «Взаємодія з
населенням на засадах партнерства», Академія патрульної поліції. Читають її
не програмісти — слухачі курсу й дільничні. Це комерційний продукт: карта має
читатися з першого погляду, без пояснень.

## Що прочитати перед роботою

1. `CILI.md` — навіщо це все. Читати першим.
2. `ZAVDANNYA.md` — робочий перелік.
3. `NAUKA.md` — стан методики.
4. Файли `ZAVDANNYA-*.md` — окремі завдання, складені в чаті. Якщо такий файл
   є, у ньому свіжіше формулювання, ніж у `ZAVDANNYA.md`.
5. `STAN.md` — глибше за технікою, але писаний 31.08 і місцями застарів.

## Мова

Усе, що бачить користувач, і всі коментарі в коді — українською.

Коментар пояснює **чому**, а не що. «Беремо медіану, бо середнє зносить одна
вулиця з тисячею ДТП» — так. «Рахуємо медіану» — ні, це й так видно з коду.
Не переписувати чужі коментарі без причини: у них зафіксовані рішення, які
інакше доведеться приймати наново.

## Межа, яку не можна переходити

Модель міряє, **що поруч**, а не через що сталася подія. Слова «причина»
в інтерфейсі немає навмисно. Гіпотезу про причину висуває людина на місці —
для цього поруч стоїть кнопка «Що поруч».

Не вигадувати пояснень, яких немає в даних. Краще сказати «ця вулиця не
входить до переліку ризикованих», ніж придумати правдоподібний чинник.

Адреса в подіях — з рішення суду. Це місце, де подію **оформлено**, і воно не
завжди збігається з місцем, де все сталося. Це вже написано на карті постійним
рядком; не прибирати.

## Як перевіряти зміни в карті

Локально, без GitHub:

```
py -3 src\step3_map.py      (або 3-MAP.bat)
PODYVYTYSYA.bat             (підіймає http://127.0.0.1:8777/)
```

Подвійним клацанням файл `site\kyiv.html` не відкривається — панель рішень
тягне сусідній файл, а браузер із диска такого не дозволяє.

**Зміни в карті перевіряються очима, а не тільки читанням коду.** Зібрати,
відкрити, клікнути те, що міняв. Якщо змінював кольори чи переліки — звірити
на конкретних адресах, названих у завданні.

## Що не чіпати без потреби

- `src/labels.py` — словники кодів статей, 27 КБ. Стабільний файл.
- `data/` — великі файли даних. Не перезбирати й не комітити без прямої потреби.
- Перенавчання моделі (крок 4) — довге. Запускається окремою галочкою
  «Перенавчити модель ризику (крок 4)» у GitHub Actions → «Оновлення карти».
  Для змін у карті й панелі воно не потрібне.

## Публікація

Комітити й пушити звичайним git. Сайт перезбирається workflow'ом
«Оновлення карти» і виходить на https://andriithis.github.io/karta-kyiv/

Коміт пишеться українською: перший рядок — що змінилося, далі абзац — чому.
Дивіться історію, там уже склався стиль.

## Карта файлів

| Файл | Про що |
|---|---|
| `src/step1_download.py`, `step1b_fabula.py` | тексти рішень, адреси, витяги фабул |
| `src/step2*.py` | геокодування, вулична мережа, чинники середовища, межі |
| `src/step3_map.py`, `tpl_*.py` | збірка карти: панель, вікна, стилі, шари |
| `src/map_problems.py` | відбір проблем (поріг 15 однорідних епізодів) |
| `src/map_layers.py` | шари ризику під подіями |
| `src/step4_engine.py` | модель ризику: навчання, перевірка, чинники вулиць |
| `src/step5_site.py`, `step6_*.py` | оглядова сторінка й три документи |
