# План звітів (розд. 25, Б) — на затвердження

Складено 27.09.2026. Код звітів не починаю, доки план не затверджено.

## 1. Будова

| Звіт | Файл | Заміняє | Для кого |
|---|---|---|---|
| «Проблеми» — перелік і сторінка на кожну | `problemy.html` (сторінка — якір `#p-<номер>`) | частину «Аналізу» | слухач, дільничний: з чого почати на місці |
| «Схожі умови» — за видами | `skhozhi-umovy.html` | «Дослідження» | викладач: що каже модель і наскільки їй вірити |
| «Стан міста» — коротко | `stan-mista.html` | «Резюме», «Аналіз» | усі: огляд за 5 хвилин |
| Методика — спільний додаток | `metodyka.html` | методичні розділи всіх трьох | хто перевіряє |

- Старі адреси переводять на нові з тими самими якорями: `doslidzhennya.html`
  (разом із `?st=` і `#t-…`) → `skhozhi-umovy.html`; `analiz.html` і
  `rezyume.html` → `stan-mista.html`.
- Вигляд світлий, як нова карта: ті самі змінні кольорів, палітра «Яскрава»
  для видів. Друк на A4: `@media print`, розриви сторінок перед кожною
  проблемою, без тіней і підкладок.
- Панель карти — три посилання: «Проблеми», «Схожі умови», «Стан міста».
- Код: `step6_*` переписується на три модулі звітів + спільна основа
  (`step6_base` лишається: завантаження, стилі, `page()`).

## 2. Одне джерело чисел

Звіти беруть події з тієї самої збірки, що й карта (`step3_map.vybir` і
точки `P`), не з бази окремо.

**Розбіжність, яку треба вирішити (❓ Андрій).** Зараз звіти рахують
**74 388** подій, а на карті видно **56 597**: різниця — 17 791 подія без
номера будинку («центри вулиць»), яких на карті немає. Пропоную: усі числа
звітів — за картою (56 597, лише точні будинки, розд. 24); події без номера —
одним рядком у розділі даних «Стану міста» («ще 17 791 подія з назвою вулиці,
але без будинку — на карту й у підрахунки не йдуть»).

Перевірка: у `check_build` — сума подій кожного звіту дорівнює сумі на карті,
інакше збірка падає.

## 3. Що в кожному звіті — поля й звідки

### «Проблеми»

| Поле | Звідки | Стан |
|---|---|---|
| Де: адреса, район, точка на міні-карті | `P[i]` (адреса, координати, `p[8]` — район) | є |
| Механізм, статті, події й роки | `map_problems` (`mech`, `arts`, `n`, `years`, `core_n`) | є |
| Події за кварталами | дата події з проходу (`ev_date`), для справ адреси | є, треба зібрати |
| Години | `e[3]` подій адреси; частка з відомою годиною | є |
| 2–3 фабули-приклади | `spravy/<район>.json`, клас адреси B, найсвіжіші | є |
| Що поруч (250 м) — перелік із відстанями | `factors.json` | є |
| Схожі умови тут: місце вулиці й виміряні чинники | `risk.json` (вулиця адреси), `street_facts` | є |
| Тип за Еком і Кларком (кривдник · жертва · місце, …) | — | **порожньо, позначено** «визначає чат проблем» |
| Хто відповідає за місце | — | **порожньо, позначено** |
| Посібник POP Center і техніки з 25 | `NAPRYAM-PROBLEMY.md`, якщо там є відповідність механізму | інакше **порожньо, позначено** |

Порожні місця — сірою рамкою з підписом, звідки їх заповнять. Нічого не
вигадується.

### «Схожі умови»

| Поле | Звідки |
|---|---|
| Чинники кожного виду: «де цього більше — подій у N разів частіше», радіус | `engine_report` → `кратність`, назви ознак (`fact_human`) — замість ваг «+0,080» |
| Точність на відкладених роках: влучність верхніх 10% вулиць і PAI | `hit_*`, `PAI_*` |
| Друга половина міста | `hit_інший_район` |
| Скільки з показаних вулиць потім справді мали події | `вулиць_показано`, `з_них_збулося` |
| Вулиці, де умови є, а подій ще не було | `risk.json` → `quiet` (модель лише середовища) |
| Яка модель на карті | одне речення: зараз «разом» (pC) до перенавчання Б1 |

Числа оновляться після Б1 самі — звіт їх не переписує руками.

### «Стан міста»

| Поле | Звідки |
|---|---|
| Обсяг і види | події карти за видами |
| Концентрація: яка частка подій на 1%, 5%, 10% адрес | події за точками `P` |
| Райони на 10 тис. мешканців | `population.json` (комірки) × межі районів |
| Час доби | `e[3]`, частка з відомою годиною |
| Динаміка за роками й кварталами — за датою події | `ev_date`; останні два місяці — «неповні» (розд. 17) |
| Перекоси даних | лише рішення суду; частка ДТП; події без номера будинку; класи адреси C і D; затримка публікації |

## 4. Помилки нинішніх звітів — як виправляю й перевіряю

| Помилка | Виправлення | Перевірка |
|---|---|---|
| Рік «2052» | ✅ `2aba162`: дата події не пізніша за рішення | немає років поза 2023–2026 |
| Дублі в топ-30 («Міхновського, 42», «Берковецька 6-Д / 6Д») | Зводити за точкою карти (координати), як карта, а не за текстом адреси | На карті дублів у топ-40 уже 0 |
| «пл. Дарницька, 1», «пл. Севастопольська, 1», «пл. Гостомельська, 1» тощо | На площах номер лише тоді, коли він є в реченні з адресою; «рухаючись по Гостомельській площі» без номера — не точний будинок, на карту не йде. Частина справжня («Дарницька площа, буд. 1» — магазин) | 20 прикладів на площу вручну; у звіті — скільки зняли |
| Внутрішні коди («ДОР_ДТП», «АЛК_…»), «без назви», «оцінка 0.03» | Людські назви механізмів і вулиць; вулицю без назви не показувати | Пошук у зібраних HTML: `[А-ЯІЇЄ]{2,3}_`, «без назви», «оцінка 0» — має бути 0 |
| «ризик», «ризикована вулиця», «прогноз ризику» | «схожі умови» | Той самий пошук слів |
| «На карту йде Б» / «на карту йде разом» | Одне речення з `engine_report` | — |
| «Дати події в даних немає», «поділ за датою рішення» | Прибрати: дата події є (розд. 17) | — |
| «Чому так може бути» з поясненнями («люди в стресі») | Прибрати; лише виміряне й «як перевірити на місці» | Пошук фрази — 0 |
| Ваги «+0,080» | Кратність «у N разів частіше» (рішення 10.09) | Пошук `\+0,\d` — 0 |
| Число подій ≠ карта | Див. п. 2 | `check_build` |

## 5. Нові розрахунки — поля, джерело, перевірка

### 5.1. Ризиковані заклади (Ек і Кларк)

Серед закладів одного типу — які кілька дають більшість подій (J-крива).

- **Поля:** тип закладу; подій у межах 50 м від кожного; частка подій, яку
  дають верхні 10% закладів; перелік верхніх із назвою й адресою.
- **Джерело:** `factors.json` (точки закладів) і події карти. **Назв закладів
  у `factors.json` немає** — треба зберігати назву OSM у кроці 2e
  (`step2e_factors`, без перенавчання моделі).
- **Обмеження:** адреса з рішення — місце оформлення (CLAUDE.md), тож
  «подія біля закладу» — це близькість, не причина; у звіті так і сказано.
- **Перевірка:** верхні 10 закладів — очима на карті; порівняння з
  випадковим розкиданням подій (скільки дали б верхні 10% випадково).

### 5.2. Повторні й сусідні події (near-repeat)

Чи йде за подією інша поблизу й невдовзі частіше, ніж випадково.

- **Поля:** за видами — надлишок пар «до 200 м і до 14 днів» проти
  випадкового; значущість.
- **Метод:** тест Нокса з перестановкою дат (99 разів), як у Near Repeat
  Calculator (Ratcliffe).
- **Джерело:** події з датою події з тексту (без неї — не беруться) і точним
  будинком.
- **Перевірка:** на даних із навмисно перемішаними датами надлишку бути не
  повинно; у звіті — лише значущі результати.

### 5.3. Хронічні проти спалахових місць

- **Поля:** для адрес із ≥ 10 подіями — у скількох кварталах із 14 (2023–2026)
  були події; «хронічне» — у половині кварталів і більше; «спалах» — понад
  половина подій в одному кварталі.
- **Джерело:** дата події.
- **Перевірка:** стійкість класу, якщо відкинути один рік.

### 5.4. Місце × година

- **Поля:** вулиці з ≥ 20 подіями з відомою годиною, де частка нічних
  (20:00–04:00) чи денних значуще більша за міську (біноміальний тест).
- **Джерело:** `e[3]` і вулиця адреси.
- **Перевірка:** частки міста збігаються з рядком «час доби» в «Стані міста».

## 6. Порядок і обсяг

1. Спільна основа, стиль і друк; переадресації; посилання в панелі.
2. «Стан міста» (найпростіший, на ньому перевіряється п. 2).
3. «Схожі умови».
4. «Проблеми» з каркасом і позначеними порожніми місцями.
5. Методика.
6. Нові розрахунки 5.1–5.4 — окремими розділами, кожен окремим комітом.

Кожен пункт — окремий коміт. Після кожного — пошук заборонених слів і
кодів та звірка чисел з картою в `check_build`.

## Що потрібно від Андрія

1. Числа звітів — за картою (56 597), події без номера — одним рядком? (п. 2)
2. Площі без номера в тексті — знімати з карти? (п. 4)
3. Назви закладів з OSM — зберігати для 5.1? (п. 5.1)
4. Затвердити план у цілому.
