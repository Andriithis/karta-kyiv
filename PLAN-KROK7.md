# План кроку 7: перегрупування видів і одне перенавчання моделі

24.09.2026. Лише план — код після затвердження. Рішення, на які спирається:
`RISHENNYA.md`, розд. 18 (види, склад, палітра), розд. 19 (після кроку 6 на
карті лише клас B), розд. 21 (прохід по текстах, дата події). Методика —
`NAUKA.md`, розд. 2–3.

## Коли

Після того, як бот проходу закінчить ДОР (щодня по 30 000, лишилося ~100
тис., тобто 3–4 запуски). До того модель навчалася б на ДТП зі старими
адресами — 56% класу C за старим витягом.

## 1. Перегрупування видів (розд. 18)

**Порядок · Торгівля · Наркотики · Насильство · Майно · Дорожній рух.**
«Середовище і майно громади» зникає; ст. 192, 154, 152 КУпАП і 297, 299,
270 ККУ — у Порядок; 197-1 ККУ — у Майно; 297, 299 — з Насильства.

| Що | Де |
|---|---|
| склад видів | `src/mech.py` (групи подібності), `src/labels.py` (`ORDER`, підписи) — лише переліки, словники кодів не чіпаємо |
| короткі назви й кольори | `tpl_core.SHORT`, `tpl_base` (палітра світла/темна з розд. 18, перевірена `validate_palette.js`) |
| статті Дорожнього руху — три частини | без змін |
| звіти кроку 6 | `step6_*.py` беруть назви видів з тих самих переліків |

Перевірка: жодна стаття не лишилася без виду; сума подій по видах до і
після однакова; палітра — знову `validate_palette.js` для всіх пар.

## 2. Дані для навчання

- Події — з проходу по текстах: адреса, клас, **дата події** (не рішення).
- Навчання й перевірка — **лише клас B** (адресу названо в описі події). C і
  D не йдуть ні в навчання, ні в перевірку: інакше модель вчиться на адресах
  лікарень і відділків, від яких ми щойно позбулися.
- Роки — за датою події. Навчання 2024 (запасне вікно 2024–2025 для рідкісних
  видів), перевірка 2025–2026 — як зараз (`step4_engine.WINDOWS`); і так само
  географічне перенесення захід → схід (NAUKA, розд. 4).

## 3. Радіус для кожного типу об'єкта (нове, за RTM)

Зараз кожен тип об'єкта рахується в колах 100, 250 і 500 м — «обрані на
око» (NAUKA, розд. 2). На перенавчанні радіус добирається для кожного типу
окремо, так, як це робить Risk Terrain Modeling.

**Як у RTM** (Caplan, Kennedy & Neudecker, 2020; утиліта RTMDx — Caplan,
Kennedy & Piza, 2013; деталі — NAUKA, розд. 3):

1. кожен чинник вимірюється кількома способами: «є в межах r» (proximity) і
   «густо в межах r» (density) — для кількох r; у RTMDx r — кроки у пів
   квартала або квартал, до 1–4 кварталів;
2. з усіх таких змінних відбір — штрафована пуассонівська регресія (elastic
   net) з перехресною перевіркою: змінні, чиї коефіцієнти стиснулися до нуля,
   випадають;
3. далі двонапрямна покрокова регресія (Пуассон і негативна біноміальна), на
   кожному кроці — BIC; зупинка, коли BIC більше не кращає; лишається одна
   «найкраща» модель;
4. вага чинника — відносний ризик, exp(коефіцієнта).

Отже в RTM радіус не задається, а **обирається даними**: з кількох
варіантів одного чинника в модель потрапляє той, що краще пояснює події, а
часто — жоден.

**У нас:**

| крок | що саме |
|---|---|
| кандидати | 50, 100, 150, 250, 400, 500 м; для кожного — «є» (0/1) і «скільки» (кількість) |
| довжина кварталу | рахуємо середню довжину відрізка вулиці Києва з `osm_risks_raw.json` і записуємо, скільки кварталів дає кожен кандидат; 50–500 м має покривати від пів кварталу до ~3 |
| відбір | для кожного виду подій окремо: elastic net Пуассон, перехресна перевірка **лише на роках навчання**; потім покроково за BIC (Пуассон або негативна біноміальна — що дає нижчий BIC) |
| одне r на тип | якщо в модель потрапили два радіуси того самого типу, лишаємо той, що дає нижчий BIC, коли інший прибрати |
| перевірка | на відкладених роках і на східній половині: PAI і частка вулиць, де події справді сталися, — поруч із чинною моделлю (100/250/500 і відбір за кореляцією). Нова модель іде на карту, лише якщо не гірша на відкладених даних |
| жодного ручного правлення | перелік типів і кандидатів записується в `NAUKA.md` **до** запуску (NAUKA, розд. 3, «практичний крок»); після результату не змінюється |

Попарні добутки ознак (зараз — до сотень) у новій моделі не беремо: RTM
їх не будує, а відбір за кореляцією з мішенню — те, до чого рецензент
вчепиться першим (NAUKA, розд. 2).

**Результат — `data/radiusy.json`:**

```json
{"blocks_m": 110,
 "po_vydah": {"Наркотики": {"зупинки": 150, "парки": 400}, "…": {}},
 "shcho_poruch": {"зупинки": 150, "бари": 100, "лікарні": 250, "…": 0}}
```

- `po_vydah` — радіус кожного типу, що лишився в моделі виду;
- `shcho_poruch` — один радіус на тип для кнопки «Що поруч»: той самий
  відбір, але по всіх подіях разом; 0 — тип не пов'язаний з подіями в
  жодному з радіусів.

**«Що поруч» читає таблицю:** кожен тип показується у своєму радіусі; межа
кола — найбільший з них; тип з 0 — усе одно показується в найменшому колі,
бо кнопка — спостереження, а не підказка моделі (так і сказано в коментарі
`showAllNear`). До перенавчання лишається 250 м для всіх.

## 4. Одне перенавчання

- Запуск — галочкою «Перенавчити модель ризику (крок 4)» в «Оновленні карти»,
  один раз, після п.1–3 у коді. Локально — лише на копії, щоб звірити числа.
- Що звіряю й кладу в `ZVIT-KROK7.md`: склад видів і число подій; обрані
  радіуси по видах (таблиця «тип → радіус» з кварталами); відносні ризики;
  PAI і влучність на відкладених роках і сході — стара модель проти нової;
  які вулиці вийшли з переліків і які зайшли, по 10 на вид.
- Знімки «було / стане»: панель з новими видами й кольорами, шар ризику
  одного виду, «Що поруч» з різними радіусами.

## 5. Порядок і зупинки

1. План — цей файл (**зупинка**: затвердження).
2. Код п.1 і п.3 (перегрупування, змінні RTM, відбір) — гілка, без пушу.
3. Перелік типів і кандидатів — у `NAUKA.md` до запуску.
4. Прогін на копії, `ZVIT-KROK7.md`, знімки (**зупинка**: перегляд).
5. Пуш, одне перенавчання в Actions, звірка живого сайту.

## Ризики

- Мало подій класу B у рідкісних видах (Торгівля, ДТП з потерпілими):
  elastic net може не залишити жодного чинника. Тоді шар ризику цього виду
  не показується (як зараз при PAI < 3), а не малюється з натяжкою.
- Кандидатів 6 радіусів × 2 способи × ~22 типи = ~260 змінних на вид —
  того самого порядку, що й в опублікованих прогонах RTMDx (192 змінні з
  24 чинників для квартирних крадіжок), тож
  перехресна перевірка тут обов'язкова.
- Довге навчання: оцінити час на копії до запуску в Actions (ліміт 6 год).
