# Що доробити далі

Робочий перелік завдань станом на **1 вересня 2026**.
Складено наприкінці довгої сесії, щоб наступну можна було почати з нуля.

> **Якщо ви — новий чат, читайте цей файл першим.**
> `STAN.md` глибший, але написаний 31.08 і не знає про зміни 1 вересня.
> Розділ 2 нижче перелічує все, що в ньому застаріло.

---

## 1. Суть проєкту за п'ять речень

Карта проблемно-орієнтованої поліцейської діяльності для Академії патрульної
поліції, побудована з відкритих даних ЄДРСР.

**Головне — пошук ПРОБЛЕМ.** Інцидент — подія, що сталася один раз, випадково,
без системної причини. **Проблема** — скупчення подібних інцидентів в одному
місці; там є причина, і її треба знайти. Слухач має навчитися бачити проблему
місця, а патрульний — отримувати щозміни конкретний об'єкт: місце, дефект,
адресат листа.

**Ризики — не мета, а інструмент.** Модель середовища будувалася, щоб перевірити
гіпотезу «причина проблеми — в облаштуванні місця». Вона пояснює приблизно
чверть проблем. Решту треба з'ясовувати виїздом. Не переставляйте ці дві речі
місцями — у цій сесії така помилка вже сталася.

Канонічний приклад користувача: його вулиця, де немає тротуару, машини стоять на
узбіччі, а діти йдуть до школи ~50 м проїжджою частиною.

---

## 2. Що зроблено 1 вересня (чого немає в STAN.md)

| Зміна | Файл | Суть |
|---|---|---|
| Перерахунок адрес виконано | `data/` | Адрес судів у даних було ~23 900, стало 2 024. Автосписок виключень скоротився з 57 рядків до 36 |
| Короткий перелік вулиць | `step4_engine.py` | `TOPN = 25` замість 1200. `TOPGRID = 200` — довший хвіст лишається лише в пам'яті для питання «чи адреса на ризикованій вулиці» |
| Новий поріг замість фільтра «краще за тему» | `step4_engine.py` | `MIN_PAI = 3.0`. Шарів на карті стало 17 замість 7. Порівняння з темою лишилося у звіті, але долі шару більше не вирішує |
| Число «збулося» | `step4_engine.py` | Зі скількох показаних вулиць події наступних років справді сталися. Є в картці, у дослідженні та в `ZVIT_DVYGUN.md` |
| Чесна назва точки | `step3_map.py` | Купа подій у центрі вулиці більше не підписується номером першої-ліпшої події. Тепер «вул. Міхновського · вся вулиця». Такі точки не потрапляють у кандидати на проблему |
| Підкладка карти | `step3_tpl.py` | CARTO вимагає ключ і малює «API KEY REQUIRED». Перейшли на звичайні плитки OpenStreetMap + CSS-фільтр `.leaflet-tile-pane` для темного вигляду |
| «Що поруч» гаситься | `step3_tpl.py` | `map.on('popupclose', ...)`. Саме popupclose, а не click: клік по позначці в Leaflet доходить і до карти |
| Крок 0 більше не валить прогін | `step0_fetch_dump.py` | 4 спроби з паузою 20 с; якщо дампа немає, але `kyiv_*.csv` є — попередження і код 0. Розбір архіву пише в `.tmp` і підмінює готовим |

**Застаріло в STAN.md:** розділ 3 (кількість шарів), розділ 7 (фільтр «краще за
тему» більше не діє), розділ 11 напрям А (порядок змінився — див. нижче).

---

## 3. Де стоїмо: числа, на які спиратися

Порахунок 1 вересня, дані за 2024 — серпень 2026.

**Проблеми** (пара «адреса + механізм», ≥15 епізодів, адреси установ виключено):

- **152 проблеми на 129 адресах** — 0,37% адрес міста
- **126 зі 152 фіксуються в усіх трьох роках** — доказ, що це не шум
- 12 адрес мають кілька проблем одночасно — найцінніша частина списку
- дві третини адрес міста дали за три роки рівно одну подію (чисті інциденти)

**Ризики як інструмент пояснення:**

- звичайні події стоять на позначених моделлю вулицях у **17%** випадків
- проблеми — у **27%**, тобто ×1,6
- у числах: **41 проблема зі 152** пояснюється середовищем, **111 — ні**

**Якість моделі:** PAI 3,5—5,9. Перенесення на іншу половину міста 30—43%
(у стихійної торгівлі 63%). Випадковий відбір дав би 10%.

**Проміжний звіт** опубліковано окремою сторінкою (артефакт «Проблемні місця
Києва»). Він містить усі ці числа з поясненнями і придатний, щоб показувати
людям, які проєкту не знають.

---

## 4. Що робити далі — за пріоритетом

### 4.1. Специфічність ризиків `[наступне за чергою]`

**Проблема.** З 425 рядків на карті (17 шарів × 25) унікальних відрізків лише
**137**. Оболонський проспект трапляється в 15 шарах із 17. Великий проспект
набирає високу оцінку для всього одразу — просто тому, що там багато людей і
багато всього.

**Що зробити.** Ранжувати не за абсолютною оцінкою, а за специфічністю: оцінка
механізму, поділена на середню оцінку всіх шарів для цього самого відрізка.
Тоді нагору піднімуться вулиці, де щось конкретне трапляється частіше, ніж
пояснює сама лише людність.

**Де.** `step4_engine.py`, побудова `rk` перед `ordr = np.argsort(-rk)`.
Потрібен другий прохід: спершу зібрати прогнози всіх шарів, потім перерахувати
ранг. Обережно: тема і механізм не повинні ділитися одна на одну.

**Виміряти після зміни.** Кількість унікальних відрізків серед 425 (зараз 137) і
частку проблем на позначених вулицях (зараз 27%). Друге не має впасти.

### 4.2. Позначки замість кружечків

Зараз чинники середовища — однакові кружечки різного кольору. Користувач просив
**літери**: М — метро, Ш — школа, Р — ринок, А — АЗС, Б — бар і так далі, щоб
типи було видно без наведення миші.

**Де.** `step3_tpl.py`, функції `drawFacts`, `showNear`, `showAllNear` —
`L.circleMarker` замінити на `L.marker` з `L.divIcon`. Обережно: карта
малюється з `preferCanvas: true`, `divIcon` піде в DOM-шар — на 5 000 об'єктів
може просісти. Або лишити кружечки, а літеру малювати всередині (Canvas цього не
вміє — тоді таки divIcon, але з обмеженням за масштабом, як зараз `FZOOM = 14`).

### 4.3. Картка проблеми на карті — слабка

Користувач порівняв зі звітом і сказав прямо: те, що відкривається за кліком,
гірше. Треба привести картку до рівня звіту: не лише «скільки подій», а
**роки повторення, розклад доби, чи це одна з 12 адрес із кількома проблемами,
чи пояснює середовище**.

**Де.** `step3_tpl.py`, блок `pblock` у `bindPopup`. Дані вже є в `probs`, окрім
розкладу — його треба покласти в `step3_map.py` разом із проблемою.

### 4.4. Розділ «проблеми ⇄ ризики» у дослідженні

Число 27% проти 17% — головний доказ, що ризики працюють. Зараз воно пораховане
лише вручну в чаті. Має рахуватися в конвеєрі й стояти в `doslidzhennya.html`
окремим розділом.

**Де.** Новий блок у `step6_*`; розрахунок — у `step3_map.py`, де вже є і
проблеми, і `theme_rgrid`.

### 4.5. Дедуплікація в `step3_map.py` `[старий борг В5]`

«Одна справа = одна подія» реалізовано лише у двигуні (`step4_engine.py`).
На карті проблеми досі рахуються за документами, тому кримінальні скупчення
перебільшені: одна крадіжка з чотирма ухвалами важить як чотири.

**Це впливає на список зі 152 проблем.** Після виправлення він зміниться —
ймовірно, поменшає в майнових і наркотичних механізмах.

### 4.6. ДТП з потерпілими — рішення про поріг

Найважливіший механізм не набирає `MIN_EV = 250` навіть на двох роках.
За 2024—2025 маємо 453 документи з точною адресою, після склеювання справ —
менше порога. Проблем цього механізму лише 3.

Два варіанти, обидва з ціною:

- **дозволити події рівня «вулиця»** (+461 документ за два роки) — точність
  падає з відрізка до вулиці;
- **опустити поріг** до ~150 — модель стає нестабільною.

Рішення не ухвалене. Обговорити з користувачем.

### 4.7. Небезпечні підходи до шкіл `[А2]`

Єдиний шар, здатний показати проблему **до першої події**: модельований потік
дітей + відсутність тротуару. Не залежить від кількості подій. Каркас у
`step4_engine.py` є (`danger`), але на реальних даних дає 0 ділянок — треба
розібратися чому (найімовірніше, `risks.json` не має шарів `no_walk`/`maybe_walk`,
бо `step2b_risks.py` не повертає `out['lines']`).

Це ж закриває питання Кадетського гаю: подій там 56 за три роки, у моделі за
кількістю подій він не з'явиться ніколи.

### 4.8. Генератор листа `[А3]`

Паспорт SARA завантажується, але лист адресатові треба писати руками. Зробити
шаблон: балансоутримувач / КП / районна адміністрація, з підставленими адресою,
механізмом, кількістю подій за роками й чинниками.

### 4.9. Решта, у порядку спадання користі

| | Завдання | Де |
|---|---|---|
| Б2 | Фабули у картці — почати з посилань `reyestr.court.gov.ua/Review/<doc_id>` | `step3_tpl.py` |
| В2 | Дата події замість дати рішення — зараз межа між роками розмита на кілька місяців | `step1_download.py` |
| А4 | Реєстр об'єктів: що взяли в роботу, що написали, що відповіли | новий файл |
| В1 | Правові назви статей у `labels.py` (файл машинно-згенерований, регенерувати) | `labels.py` |
| В4 | Нормалізація адрес: «вул. Я.Коласа, 27» і «вул. Якуба Коласа, 27» — різні точки | `addr.py` |
| Б1 | Явний навчальний маршрут слухача | `step6_*` |
| Г1 | Стаття: попередньо зареєстрована методика + порівняння з базовою лінією «топ місць за минулими подіями» | — |

### 4.10. Відкриті питання до користувача

1. **Модель В окремим шаром?** Зараз на карту йде Б (лише середовище), бо тільки
   вона вказує на вулиці без історії. В (історія + середовище) точніша, але
   показує те, що й так видно точками. Обговорення відкладено — користувач
   сказав «пізніше проговоримо».
2. **TOPN = 25 — це правильне число?** Змінюється одним рядком у
   `step4_engine.py` або змінною оточення `TOPN`.
3. **Хостинг викладацької версії** — випадкова адреса, пароль на клієнті чи
   лишити як є.

---

## 5. Правила роботи з цим репозиторієм

Важливо для нового чату — на це вже витрачався час.

**Заливання коду.** `git push` заборонений проксі. Єдиний шлях —
`mcp__GitHub__push_files` з повним вмістом файлу. Токен **не має scope
`workflow`**, тому `.github/workflows/update.yml` редагує лише користувач
вручну. Будь-яке виправлення конвеєра має вміщатися в `src/`.

**Після кожного push — перевірка.** Символ `і` (U+0456) зрідка перетворюється на
`ᑖ` (U+1456). Обов'язково:

```
git clone --depth 1 https://github.com/Andriithis/karta-kyiv.git /tmp/chk
diff локальний_файл /tmp/chk/src/файл
python3 -m py_compile /tmp/chk/src/*.py
```

Для `step3_tpl.py` додатково витягнути `<script>` зі згенерованої `karta.html` і
перевірити `node --check`, а розмітку — балансом тегів через `HTMLParser`.

**Мережа з пісочниці.** `reyestr.court.gov.ua`, `overpass-api.de` та
`api.github.com` через curl недоступні. Працюють лише WebFetch/WebSearch,
MCP та `git clone`. Тому двигун і карту тестуємо на синтетичному місті —
воно лежало в `/tmp/eng` і при потребі збирається заново з реального набору
подій.

**Мова.** Користувач просив писати українською й називати пункти інтерфейсу
їхніми українськими підписами, а не англійськими ідентифікаторами.

**Тон.** Коротко й простими словами. Довгі пояснення користувач зупиняв прямо.

---

## 6. Журнал рішень 1 вересня

- **Прибрано фільтр «механізм має бути точнішим за тему».** Різниця 38,0% проти
  38,4% — шум, а ціною була відсутність шару «ДТП» на карті. Замінено порогом
  власної якості `MIN_PAI = 3.0`. Порівняння з темою лишилося у звіті й
  називається прямо.
- **1200 вулиць у шарі скорочено до 25.** У хвості того переліку події були
  лише на 26% вулиць — перелік створював видимість знання.
- **Центр вулиці більше не вважається місцем.** Коли будинку немає в OSM, подія
  стає в центр вулиці; таких куп на карті було багато, і вони підписувалися
  чужим номером будинку. Тепер підписуються чесно і в проблеми не потрапляють.
- **Звіт перебудовано навколо проблем.** Перша редакція була про ризики —
  користувач вказав, що це переставляє мету й інструмент місцями.

---

*Оновлено 1 вересня 2026. Попередній, докладніший опис стану — `STAN.md`.*
