# Завдання: новий вигляд карти

Складено 15.09.2026. Рішення й обґрунтування — у `RISHENNYA.md`
(розділи 1–9). Тут порядок робіт і те, що треба знати, щоб їх зробити.

**Правило роботи: один крок — один коміт.** Після кожного кроку зібрати
карту локально (`py -3 src\step3_map.py`, потім `PODYVYTYSYA.bat`),
подивитися очима й показати Андрієві. Не робити кроки 1–5 одним заходом.

Макет із трьома темами Андрій дивиться окремо — описане нижче звідти.

---

## Крок 1. Колір маркера і перелік проблем

Повністю описано в `ZAVDANNYA-KOLIR.md`. Коротко: колір береться з першої
події в масиві (`tpl_popup.py:223`), має братися з переважної теми;
адреса-проблема фарбується кольором свого напрямку і не міняє колір від
фільтрів; режим «Тільки проблеми» має лишати адресу, лише якщо видимий
той самий напрямок, за яким її відібрано.

Це робиться першим, бо ламає читання карти просто зараз.

---

## Крок 2. Три теми й нова палітра

### 2.1. Прибрати інверсію

Зроблено 16.09.2026. Фільтр `.leaflet-tile-pane{filter:invert(1)…}` прибрано:
підбирати фільтри до OSM не довелося, бо кожна тема дістала **свій набір
плиток** (`TILES` у `tpl_map.py`).

| Тема | Плитки | Атрибуція |
|---|---|---|
| Світла | Esri World Light Gray | Esri, HERE, Garmin, © OpenStreetMap |
| Темна | CARTO Dark Matter | © OpenStreetMap, © CARTO |
| Кольорова | CARTO Voyager | © OpenStreetMap, © CARTO |

`CARTO_KEY` у `tpl_map.py` — порожній рядок. Так CARTO віддає плитки з
написом **API KEY REQUIRED** поверх карти; вставлений ключ його прибирає.
Ключ клієнтський, він не секрет.

Про Esri треба знати: це старий відкритий REST-ендпоїнт, яким карти
користуються роками, але умови Esri формально передбачають обліковий запис.
Для навчального інструменту ризик малий. **Цільова світла підкладка — CARTO
Positron**: щойно з'явиться ключ, `tiles()` сама перемкнеться на неї.

### 2.2. Кольори інтерфейсу — через змінні CSS

Три набори на `:root[data-theme=...]`. Панель, підписи, лінії — усе бере
колір зі змінних, ніде не лишається літералів.

Набори взято з `PROBA-VYGLIADU.html` — значення нижче вже стоять у коді.
Набір обирає атрибут `data-t` на `<body>`, не `:root`.

| Змінна | Світла | Темна | Кольорова |
|---|---|---|---|
| `--panel` | `#fff` | `#0f1217` | `#12161b` |
| `--sunk` | `#f7f7f5` | `#151920` | `#181d23` |
| `--ink` | `#14161a` | `#eaebef` | `#eaebef` |
| `--dim` | `#5f6771` | `#8d94a2` | `#8d94a2` |
| `--faint` | `#9aa0a9` | `#5b6371` | `#5f6775` |
| `--rule` | `#e7e6e3` | `#1f242b` | `#212730` |
| `--ground` (тло карти, затемнення) | `#f6f6f4` | `#0c0e12` | `#1a2430` |
| `--halo` (обвідка маркера) | `#fff` | `#0b0d11` | `#101820` |
| `--glass` | `rgba(255,255,255,.80)` | `rgba(15,18,23,.78)` | `rgba(18,22,27,.78)` |
| `--shadow` | `drop-shadow(0 1px 1.5px rgba(16,18,22,.22))` | `drop-shadow(0 1px 2px rgba(0,0,0,.55))` | `drop-shadow(0 1px 2px rgba(0,0,0,.5))` |

Шрифти звідти ж: `--sans` Commissioner, `--mono` IBM Plex Mono,
`--disp` Unbounded.

### 2.3. Палітра тем правопорушень

Порядок — як у `M.groups`. Два набори: темніший для світлої підкладки,
світліший для темної й кольорової.

Палітру перевірено на розрізнення при дальтонізмі — обґрунтування в
`RISHENNYA.md`, розділ 2. Ці значення вже стоять у `PAL` (`tpl_map.py`).

| # | Тема | Світла | Темна / кольорова |
|---|---|---|---|
| 0 | Громадський порядок | `#eb6834` | `#d95926` |
| 1 | Алкоголь і торгівля | `#1baf7a` | `#199e70` |
| 2 | Наркотики | `#4a3aa7` | `#9085e9` |
| 3 | Насильство проти особи | `#e34948` | `#e66767` |
| 4 | Майнові | `#2a78d6` | `#3987e5` |
| 5 | Дорожній рух | `#008300` | `#008300` |
| 6 | Середовище громади | `#e87ba4` | `#d55181` |

Восьмим у масиві стоїть запас для домашнього насильства (`#7a6f63` /
`#8d94a2`): у `M.groups` воно не входить, але без запасу залишок від ділення
віддавав би йому колір громадського порядку.

Чинна палітра `PALA` (`tpl_map.py:15`) прибирається: у ній громадський
порядок `#e0533d` і насильство `#ef4444` — майже однаковий червоний.

### 2.4. Перемикач

Три слова у правому верхньому куті карти: **Світла · Темна · Кольорова**,
активне підкреслене. Вибір зберігати в `localStorage`.

---

## Крок 3. Панель

360 px, **праворуч** (зараз ліворуч). Три блоки замість десяти.

1. **Режим** — рядок угорі без заголовка: Усі · Проблеми · Теплова.
   Активний підкреслений, не залитий.
2. **Правопорушення** — один перелік, у кожному рядку:
   `[квадрат] Назва ....... число   [риска] точність`
   - квадрат кольору теми вмикає **події**; вимкнений — порожній контур;
   - риска того самого кольору вмикає **прогноз ризику** цієї теми;
   - точність шару дрібним моноширинним праворуч від риски, видима лише
     коли ризик увімкнено.
   Окремого блока «Прогноз ризику» більше немає.
3. **Контекст** — один вимикач «Об'єкти довкола» на все середовище
   плюс рядок дрібним переліком груп. Поділу «притягують / збирають
   людей» немає.
4. **Район** — дрібні підписи в кілька рядків, активний підкреслений.
5. **Підвал** — три документи окремими рядками з підписом формату, під
   ними один рядок дрібним: «Адреса — з рішення суду: місце, де подію
   оформлено. © OpenStreetMap».

Прибрати: блок «Топ адрес» (переїздить у звіт), фільтр за судом, блок
«Рік», блок «Час доби», **усі пояснювальні абзаци** (зараз їх сім,
близько 750 знаків).

Стиль: жодних залитих кнопок, карток, тіней і заокруглень. Активний стан
— підкреслення. Волосяна лінія `--rule` між рядками.

---

## Крок 4. Період — на карту

Прибрати з панелі, поставити у **лівий верхній кут карти**:

```
‹   Січень 2026   ›     [ Місяць ▾ ]
```

- стрілки гортають на крок назад/вперед;
- випадайка міняє крок: **Усе · Рік · Квартал · Місяць**. Тижня немає.
- за замовчуванням — **2026 рік**.

Період фільтрує **події**: маркери, числа, теплову карту, розклад доби.
Перелік проблем від нього не пересортовується.

Так зроблено в police.uk і CrimeMapping: час — це те, чим гортають карту.

---

## Крок 5. Зведення у вікні

У тому самому лівому куті, над періодом — кільце:

- усередині — **кількість подій у поточному вікні карти** (не в місті);
- під числом дрібним підпис «**подій у вікні**» — обов'язково, інакше
  число читатимуть як міське;
- кільце складене з дуг за темами, кольори ті самі;
- праворуч від кільця — п'ять найбільших тем із відсотками.

Перераховується на `moveend`/`zoomend` по тому, що потрапляє в
`map.getBounds()` і проходить поточний фільтр. Відсунули — тисячі подій,
наблизилися до кварталу — двадцять.

---

## Крок 6. Кластери

`Leaflet.markercluster`. Кластер — кружечок із числом, обведений
**кільцем із дуг за темами**.

**Кольори не змішувати.** Зелений (дорожній рух) + червоний (громадський
порядок) дають оливковий, а оливковий у нас уже належить темі «Алкоголь
і торгівля» — кластер із двох тем виглядав би як третя.

Адреси з відібраними проблемами **в кластер не потрапляють**: лишаються
видимими на будь-якому масштабі, інакше карта суперечитиме переліку.

---

## Крок 7. Стовпчики

Від масштабу 16 адреса малюється не колом, а стовпчиком: висота —
кількість, під ним легкий еліпс-тінь. Робиться в Leaflet (`divIcon` або
малювання в канві). Справжнє 3D означало б перехід на MapLibre GL і
перепис усіх шарів — не зараз.

---

## Крок 8. Прибрати паспорт SARA

Кнопку з картки проблеми, `buildPassport` і `downloadPassport` із
`tpl_popup.py`.

---

## Крок 9. Освітлення, переходи, тротуари, парки — у модель

`step2b_risks.py` уже качає з OSM ліхтарі, переходи, заспокоєння руху,
пішохідні шляхи й парки. У моделі (83 ознаки в `engine_report.json`)
немає жодної з них.

У коді, рядок 294, стоїть `out['lines'] = {}` із поясненням, що дані OSM
про ВІДСУТНІСТЬ ненадійні. Це правильно — тому ознака має бути не
«тут немає освітлення», а **щільність**, як усі інші робочі ознаки:

- `ліхтарі_100м`, `ліхтарі_250м`
- `переходи_250м`
- `заспокоєння_250м`
- `пішохідні_шляхи_100м`
- `парки_250м`

Знак модель визначить сама. Додати в `step2e_factors.py` і в перелік
`NAMES` кроку 4, перенавчити, подивитися у звіті, чи ознаки вижили.

Заразом: у `risks.json` категорії **лавки, камери, дерева, трава**
стоять із нулем — вони мають назви в коді, але запитів до OSM для них
немає. Камери (`man_made=surveillance`) варто додати.

Перед тим як додавати `surface`, `smoothness`, `maxspeed`, `lit`,
`landuse` — **порахувати покриття по Києву через Overpass**. Ознаку,
розмічену на 3% вулиць, у модель брати не варто.


---

---

## Крок 10. Нове визначення проблеми

Найбільше й останнє. Зараз проблема — це «15 однорідних епізодів»,
тобто просто кількість. Має бути: **місце, де подій істотно більше, ніж
очікується саме для такого місця**.

- очікуване береться з моделі середовища (крок 4): вона вже знає
  населення, потоки, зупинки, заклади, клас дороги;
- ранжування — за перевищенням, із нижньою довірчою межею, щоб «3 події
  там, де очікувалося 0,2» не стояли вище за «90 там, де очікувалося 30»;
- нижня межа кількості лишається, але низька;
- у картці: «тут N подій; для такого місця очікується E; це у X разів
  більше».

Перевірено на даних: нормувати на саме лише населення не можна — у
верхівці опиняються адреси з нульовим населенням, і всі вони вже в
`vykluchennya.txt` (суди й відділки). Нормувати після виключень і з
підлогою експозиції. Для дорожнього руху знаменник — рух, не люди; саме
тому очікуване беремо з моделі, а не ділимо на населення.

Деталі — `RISHENNYA.md`, розділ 8.

---

## Крок 11. Фабули — місце і якість

### Де вони тепер живуть

Раніше рішення відкривалися праворуч. Тепер праворуч — панель фільтрів,
тому фабули переїжджають на **ліве скляне полотно, те саме, де дашборд**:
напівпрозоре тло з розмиттям, крізь нього видно карту. Одне полотно —
два вмісти: натиснули кільце — дашборд, натиснули адресу — картка адреси
з фабулами. Відкриття одного закриває друге.

У картці: адреса, скільки подій за поточним фільтром і яка тема
переважає, перелік статей із числами, далі фабули — дата, стаття,
текст, посилання на повний документ.

Як це виглядає — у `PROBA-VYGLIADU.html`, натисніть будь-яку точку.

### Якість фабул

**Знімок на диску досі зі старим різанням.** У шапці `data/fabuly.csv.gz`
лише `doc_id` і `txt` — позначки `SNAP_RULE` немає, отже нове правило
жодного разу не застосовувалося. Воно спрацює при завантаженні, але
ніхто його ще не запускав. Перше, що треба зробити, — запустити й
подивитися на результат.

**Чого нове правило ще не ловить.** На вибірці з 8 витягів видно три
процесуальні вступи, які лишаються:

- «Як вбачається з протоколу про адміністративне правопорушення серії
  ЕПР1 № 493049 від …»
- «згідно із протоколами про адміністративні правопорушення серії ААД
  №853170 від … та серії ААД №853171 від …»
- «На розгляді Печерського районного суду м. Києва перебуває вказане
  кримінальне провадження. Так, ОСОБА_3 обвинувачується в тому, що він:»

Правило `_drop_preamble` знає лише «До … суду надійшов протокол».
Треба додати ці три форми й перевірити на знімку, скільки витягів
починається не з події.

**Що ще прибрати з тексту:**

- серії й номери протоколів («серії ААД №853170»);
- марку й серійний номер приладу («приладу Dr?ger Alcotest 6820
  № ARHK-0522»), опис відеофіксації, перелік свідків.

**Окремо: у даних є пошкоджена кодування.** «Dr?ger» — це «Dräger», у
якого загубилася умлаут-літера десь на кроці 1. Варто знайти, де саме
текст втрачає не-латинські символи: якщо це трапляється з «ä», то
трапляється й з іншим.

**Мірило готовності.** Фабула має починатися з того, **хто, коли, де і
що зробив**, і закінчуватися до першого доказового звороту. Перевірка
проста: взяти 20 випадкових витягів і прочитати вголос. Якщо в якомусь
перше речення — про протокол, а не про подію, правило ще не готове.
