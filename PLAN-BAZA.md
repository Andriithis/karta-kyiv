# План: власна база подій (етап 6б)

24.09.2026, друга редакція — з рішеннями центрального чату того ж дня.
Перша редакція прийнята як основа. Код кроку 1 (збереження текстів) уже
працює (`ca07f5c`); кроки 2–3 — після перегляду цього плану.

База — лише для цього навчального проєкту. З «Моєї вулиці»
(`Andriithis/moya-vulytsya`) взято ідеї будови (справа → документ → подія
→ місце, ролі адрес, хеш рядка реєстру). Код і дані не копіюються: у нас
SQLite без сервера й власний розбір адрес (`addr.py`, `step1c_teksty.py`).

## Навіщо

Подія на карті збирається на льоту з семи джерел:
- `events.csv.gz`, `formy.csv.gz`, `posylannya/`;
- частини проходу;
- `step2_geocode`, `map_excl`;
- `merge_cases`.

Карта, звіти й модель повторювали цей ланцюг кожен по-своєму, і звіти
розійшлися з картою в 2,3 раза (виправлено 24.09 через
`step3_map.vybir()`). Модель досі вчиться на документах.

Повний текст прохід качав і викидав. Нове правило бачило лише фабулу,
обрізану до 1 000 / 2 500 знаків.

## Порядок (рішення 24.09)

1. **Збереження повних текстів** — зроблено, див. нижче.
2. **Таблиця подій із ролями адрес** — `data/baza.sqlite` в Actions, версії
   — у приватному репозиторії.
3. **Карта, звіти й модель читають лише з таблиці.** Модель — разом із
   кроком 7.

Після кожного кроку — звіт і пуш.

## 1. Повні тексти — зроблено

- Прохід кладе кожен завантажений текст у
  `vykhid/teksty/<місяць рішення>.jsonl.xz`, по рядку на рішення:
  `doc_id, cat, sud, data, ref, sha256, tekst`.
- Постійне місце — реліз `teksty` приватного репозиторію
  `Andriithis/karta-kyiv-dani`, місячними архівами. Архів дописується:
  старий файл і новий xz-потік підряд — теж коректний `.xz`.
- Доки немає секрету `DANI_TOKEN`, тексти лягають в артефакт запуску на 90
  днів. **Репозиторій `karta-kyiv` публічний, тож такий артефакт може
  завантажити будь-хто, хто увійшов у GitHub.** Тимчасово, до токена;
  інструкція — `INSTRUKTSIYA-DANI.md`.
- **Уже викинуто 30 000 текстів** (перша частина бота, 23.09):
  ГП 9 365, НАР 8 728, ДОР 4 282, МАЙ 3 750, АЛК 2 760, НАС 876,
  СЕР 239. Їх докачати окремим запуском — ~4 год в Actions. Решта черги
  (100 141 документ, переважно ДОР) тепер зберігається по ходу.
- Зберігаємо текст, а не RTF. Якщо колись зміниться `rtf_to_text`, текст
  доведеться качати наново; RTF утричі більший.

Розміри (виміряно 24.09 на 40 рішеннях):

| | КУпАП | КК |
|---|---:|---:|
| текст, знаків (середнє) | ~6 000 | ~25 000 |
| текст у gzip, по одному | 2,9 КБ | 7,4 КБ |
| подій | 120 123 | 13 494 |

- Разом ~440 МБ, якщо стискати по одному. Місячними xz-архівами очікую
  150–250 МБ; на 20 пробних рішеннях вийшло ~1,5 КБ на текст.
- Ліміт GitHub — 2 ГБ на файл релізу.

## 2. Таблиця подій

### Будова (SQLite, файл `baza.sqlite`)

| таблиця | рядок | ключові поля |
|---|---|---|
| `sprava` | справа | `nomer` (cause_num), `sud` |
| `dokument` | рішення реєстру | `doc_id`, `sprava`, `forma` (вирок / постанова / ухвала), `cat`, `data_rishennya`, `ref`, **`hash_ryadka`** (sha256 рядка реєстру), **`hash_tekstu`**, `potribna_obrobka` |
| `podia` | подія = справа × вид | `sprava`, `vyd`, `stattya`, `statti`, `doc` (представник), `data_podii`, `chas_podii`, `fabula`, `vada`, **`pravylo`** (версія правил, `v3`…), `klas` (виводиться з ролей) |
| `misce` | адреса як сутність | `vulytsya`, `budynok`, `norm` (ключ зіставлення), `lat`, `lon`, `tochnist` (house / base / interp / cross / street), `ustanova` (так / ні, з якого переліку), `u_misti` (так / ні — межа Києва) |
| `podia_misce` | зв'язок з роллю | `podia`, `misce`, **`rol`**, `dokaz` (цитата-речення), `pozytsiya` (зміщення в тексті) |
| `versiya` | прогін | `pravylo`, `data`, скільки документів оброблено / змінено |

- Щотижня обробляються лише документи, у яких змінився `hash_ryadka` (новий
  чи виправлений у реєстрі) або `pravylo` старіше за чинне.
- Нове правило перераховується з архіву текстів, без реєстру.

### Ролі адрес

Зберігаються **всі** адреси, які розбір знаходить у тексті, кожна з роллю
й цитатою, а не лише одна.

| роль | як розпізнається (з уже наявних правил `addr.py`) |
|---|---|
| `EVENT_LOCATION` | чинне правило «перша чиста згадка в описі події» (+ НАР: не місце замовлення, `v3`) |
| `COURT` | контекст «суд», шапка рішення (зараз — NOISE) |
| `RESIDENCE` | «проживає», «зареєстрований», «місце проживання» |
| `WORKPLACE` | «працює», «місце роботи» |
| `PROPERTY` | «належить», «квартира / будинок потерпілого», майно |
| `INSTITUTION` | відділ поліції, лікарня, експертиза, установа з переліку |
| `OTHER` | решта згадок |

Клас із ролей:
- **B** — є `EVENT_LOCATION` з вулицею, названою в реченні події;
- **C** — адреса події є, але описом не підтверджена (є лише `OTHER` чи
  адреса з документа, а не з речення події);
- **D** — `EVENT_LOCATION` прихована (АДРЕСА_N), фабула шкідлива або є лише
  `COURT`, `RESIDENCE`, `WORKPLACE`, `PROPERTY`, `INSTITUTION`.

Карта бере `EVENT_LOCATION`. Решта ролей — для перевірки й для слухачів
(«адресу суду не показуємо, бо…»).

### Координати, установи, межа

- **Координати — у таблиці `misce`.** Геокодування — як зараз
  (`step2_geocode`: будинок, «20Б → 20», між сусідами, перехрестя), але
  один раз на адресу, а не на документ.
- **Установи — позначкою `ustanova`, не вилученням.** Карта й звіти їх
  фільтрують; скільки відфільтровано — видно.
- **Межа міста** — GeoJSON Києва (адмінмежа OSM, уже є в `borders.json`
  по районах). Точка поза межею — `u_misti = ні`, на карту не йде.

### Перейменовані вулиці

- Довідник `data/pereimenuvannya.csv`: `stara_nazva, nova_nazva, data,
  rishennya, dzherelo` (посилання на сторінку kyivcity.gov.ua з
  конкретним рішенням). Перелік Києва 2014–2025, з джерелом для кожного
  запису.
- Геокодування шукає й стару, й нову назву: у текстах 2024 року ще
  трапляються старі назви, а в OSM — нові.
- Підпис точки — нова назва.
- Що з'ясувати: у якому вигляді kyivcity.gov.ua публікує перелік (сторінки
  рішень чи зведена таблиця). Якщо зведеної немає — збирати з рішень
  Київради, для кожного запису — посилання.

### Де лежить і хто оновлює

- `baza.sqlite` збирається в Actions. Його версії за датою
  (`baza-2026-09-28.sqlite.xz`) — у приватному `karta-kyiv-dani`, реліз
  `baza`. Основний репозиторій не роздувається.
- Сайт отримує лише те, що показує: `kyiv.html` і `spravy/*.json`, як
  зараз.
- Хто оновлює:
  - **до кінця черги** — щоденний «Прохід по текстах», як зараз;
  - **після** — усе в щотижневому «Оновленні карти»: докачка нових
    рішень → тексти в архів → обробка → база → карта.
- Обидва в одній черзі `concurrency: karta-update`.

## 3. Хто читає таблицю

- `step3_map` — `vybir()` замінюється читанням `podia` + `misce` (роль
  `EVENT_LOCATION`, не установа, у межах міста, дата від 2023).
- Перевірка: ті самі 61 989 подій і ті самі точки, що зараз, або пояснена
  різниця.
- `step6_*` — уже читають `vybir()`, перейдуть разом.
- `step4_engine` — на кроці 7.

## Час

| що | скільки |
|---|---|
| докачати 30 000 уже викинутих текстів | ~4 год в Actions, один запуск |
| решта черги (100 тис.) | щоденний бот, ~4 запуски, тексти зберігаються |
| перша збірка бази з архіву текстів | оцінка 1–2 год (повний розбір 133 тис.) — заміряти на кроці 2 |
| тиждень нових рішень (~900) | хвилини |

## Відкриті питання

1. Скільки ролей розпізнається правилами надійно. Перед кроком 3 —
   вибірка 30 документів з усіма знайденими адресами й ролями, оцінка
   очима, як для фабул.
2. Документи-ухвали й інші папери справи (~75 тис.) — чи качати їхні
   тексти. Для подій не потрібні; для «Усі рішення» досить посилання.
3. Форма зведеного переліку перейменувань на kyivcity.gov.ua (див. вище).
