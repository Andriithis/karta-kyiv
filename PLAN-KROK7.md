# План кроку 7 (Б1): перегрупування видів і одне перенавчання моделі

24.09.2026, **затверджено Андрієм 25.09.2026 з поправками** (позначено
«25.09»). Рішення, на які спирається: `RISHENNYA.md`, розд. 18 (види й
склад), 19 (клас B), 21 (прохід, дата події), 23 (палітри нової карти),
24 (єдині правила бази). Методика — `NAUKA.md`, розд. 2–3. Місце в порядку
— `PLAN-ZAHALNYI.md`, крок Б1.

**Принцип (Андрій, 25.09): робимо по-науковому, щоб було валідно, і в
будь-якому разі нова модель іде на карту.**

## Коли

Після того, як бот проходу закінчить ДОР (щодня по 30 000, лишилося кілька
запусків). До того модель навчалася б на ДТП зі старими адресами. Базу
(`dani-edrsr`) модель **не чекає**: на неї переходить разом з картою (А5).

## 0. Що на карті — «Схожі умови» (25.09)

- На карті шар називається **«Схожі умови»**, не «ризик» і не «прогноз»:
  він показує, де середовище схоже на місця з подіями, а не передбачає
  злочин (`VYZNACHENNYA-PROBLEMY.md`, розд. 7.2 і 9).
- На карту йде **модель лише середовища** (у коді `pB`), як у Risk Terrain
  Modeling: вона не знає, де події вже були, тож підсвічує й вулиці, де
  подій ще немає, а умови ті самі. Моделі «історія» і «разом» лишаються у
  звіті поруч — для чесного порівняння, не на карті.
- Слово «причина» в інтерфейсі не вживаємо (`CLAUDE.md`).

## 1. Перегрупування видів (розд. 18)

**Порядок · Торгівля · Наркотики · Насильство · Майно · Дорожній рух.**
«Середовище і майно громади» зникає; ст. 192, 154, 152 КУпАП і 297, 299,
270 ККУ — у Порядок; 197-1 ККУ — у Майно; 297, 299 — з Насильства.

| Що | Де |
|---|---|
| склад видів | `src/mech.py` (групи подібності), `src/labels.py` (`ORDER`, підписи) — лише переліки, словники кодів не чіпаємо |
| короткі назви | `tpl_core.SHORT` |
| кольори (25.09) | палітри нової карти з розд. 23 (Яскрава, Крейдяна, Графітова), колір прив'язаний до **назви** виду; сьомий (`#DD7DA2`) відпадає. Таблиця розд. 18 скасована; стару карту не перефарбовуємо |
| статті Дорожнього руху — три частини | без змін |
| звіти | `step6_*.py` беруть назви видів з тих самих переліків |

Перевірка: жодна стаття не лишилася без виду; сума подій по видах до і
після однакова.

## 2. Дані для навчання

- Події — з проходу по текстах: адреса, клас, **дата події** (не рішення).
- Навчання й перевірка — **лише клас B** (адресу названо в описі події).
- **Лише точний будинок (25.09, розд. 24):** точки «20Б → 20» (`base`),
  «між сусідами» (`interp`), перехрестя й центр вулиці в навчання й
  перевірку не йдуть. Так модель одразу на правилах бази, і після
  переходу на базу її не треба вчити вдруге.
- Роки — за датою події. Навчання 2024 (запасне вікно 2024–2025 для рідкісних
  видів), перевірка 2025–2026 (`step4_engine.WINDOWS`); географічне
  перенесення захід → схід (NAUKA, розд. 4).
- У звіті — скільки подій на кожен вид лишилося після цих фільтрів.

## 3. Радіус для кожного типу об'єкта (за RTM)

Зараз кожен тип об'єкта рахується в колах 100, 250 і 500 м — «обрані на
око» (NAUKA, розд. 2). На перенавчанні радіус добирається для кожного типу
окремо, так, як це робить Risk Terrain Modeling.

**Як у RTM** (Caplan, Kennedy & Neudecker, 2020; утиліта RTMDx — Caplan,
Kennedy & Piza, 2013; деталі — NAUKA, розд. 3):

1. кожен чинник вимірюється кількома способами: «є в межах r» (proximity) і
   «густо в межах r» (density) — для кількох r;
2. з усіх таких змінних відбір — штрафована пуассонівська регресія (elastic
   net) з перехресною перевіркою: змінні, чиї коефіцієнти стиснулися до нуля,
   випадають;
3. далі двонапрямна покрокова регресія (Пуассон і негативна біноміальна), на
   кожному кроці — BIC; зупинка, коли BIC більше не кращає;
4. вага чинника — відносний ризик, exp(коефіцієнта); на карті й у звітах —
   кратність («у N разів частіше»), не коефіцієнт.

**У нас:**

| крок | що саме |
|---|---|
| кандидати | 50, 100, 150, 250, 400, 500 м; для кожного — «є» (0/1) і «скільки» (кількість) |
| довжина кварталу | середня довжина відрізка вулиці Києва з `osm_risks_raw.json`; скільки кварталів дає кожен кандидат |
| відбір | для кожного виду окремо: elastic net Пуассон, перехресна перевірка **лише на роках навчання**; потім покроково за BIC |
| одне r на тип | якщо в модель потрапили два радіуси того самого типу, лишаємо той, що дає нижчий BIC, коли інший прибрати |
| жодного ручного правлення | перелік типів і кандидатів записується в `NAUKA.md` **до** запуску; після результату не змінюється |

Попарні добутки ознак у новій моделі не беремо: RTM їх не будує.

**Результат — `data/radiusy.json`:**

```json
{"blocks_m": 110,
 "po_vydah": {"Наркотики": {"зупинки": 150, "парки": 400}, "…": {}},
 "shcho_poruch": {"зупинки": 150, "бари": 100, "лікарні": 250, "…": 0}}
```

- `po_vydah` — радіус кожного типу, що лишився в моделі виду;
- `shcho_poruch` — один радіус на тип для кнопки «Що поруч»: той самий
  відбір по всіх подіях разом; 0 — тип не пов'язаний з подіями.

**«Що поруч» читає таблицю:** кожен тип показується у своєму радіусі; тип з
0 — у найменшому колі, бо кнопка — спостереження, а не підказка моделі.

## 4. Перевірка — валідність нової моделі (25.09)

- **Нова модель іде на карту в будь-якому разі.** Умови «не гірша за
  стару» немає: стару вчили на хибних адресах і датах рішень, тож
  порівняння з нею — лише в звіті, для чесності.
- Валідність — перевіркою самої нової, на даних, яких вона не бачила:
  - **за часом:** навчання 2024 → перевірка 2025–2026;
  - **за місцем:** навчання на західній половині → перевірка на східній.
- Міри: **PAI** (частка спійманих подій ÷ частка виділених вулиць) і
  **влучність** (частка майбутніх подій на виділених вулицях); для частих
  видів — також очікувана кількість подій на вулицю (аудит 10.09, розд. 10).
- **Шар виду не показується**, якщо на відкладених даних PAI < 3 або
  elastic net не лишив жодного чинника — пишемо «для цього виду даних
  замало», а не малюємо з натяжкою.

## 5. Одне перенавчання

- Запуск — галочкою «Перенавчити модель ризику (крок 4)» в «Оновленні карти»,
  один раз, після п.1–3 у коді. Локально — лише на копії, щоб звірити числа.
- `ZVIT-KROK7.md`: склад видів і число подій після фільтрів; обрані радіуси
  по видах (таблиця «тип → радіус» з кварталами); кратності; PAI і
  влучність за часом і за місцем — нова модель (середовище, історія,
  разом) і стара поруч; які вулиці вийшли й зайшли, по 10 на вид; які види
  не пройшли PAI ≥ 3.
- Знімки «було / стане»: панель з новими видами й кольорами, шар «Схожі
  умови» одного виду, «Що поруч» з різними радіусами.

## 6. Порядок і зупинки

1. ~~План — затвердження~~ — 25.09.
2. Код п.1–3 — гілка, без пушу.
3. Перелік типів і кандидатів — у `NAUKA.md` до запуску.
4. Прогін на копії, `ZVIT-KROK7.md`, знімки (**зупинка**: перегляд).
5. Пуш, одне перенавчання в Actions, звірка живого сайту.

## Ризики

- Лише точні будинки класу B — у рідкісних видах (Торгівля, ДТП з
  потерпілими) подій може лишитися мало: тоді шар виду не показується
  (п. 4), а не малюється з натяжкою. Адресна база Києва (`PLAN-ZAHALNYI`,
  А4а) може повернути частину подій — тоді перенавчання вже на базі.
- ~260 змінних на вид — того самого порядку, що в опублікованих прогонах
  RTMDx, тож перехресна перевірка обов'язкова.
- Довге навчання: оцінити час на копії до запуску в Actions (ліміт 6 год).
